# 2026-08-15

## 迁移到 pi（claude/opencode → pi 单模式）

**背景**：决定全部开发迁移到 pi。serenique 从 Claude Code + opencode 双配置迁移为 pi 单模式，项目记忆重构为 registry 形式（`.pi/APPEND_SYSTEM.md` + `.pi/extensions/memory.ts` 自动注入）。

**改动**（commit `docs: migrate agent tooling from claude/opencode to pi`）

- **删除**：`.claude/`（agents/hooks/skills/settings）、`.opencode/`（agents/plugin/skills）、`CLAUDE.md`、`AGENTS.md`、`opencode.json`、`.omo/`、`.superpowers/`、`.worktrees/`、`.codegraph`
- **新增 `.pi/`**（纳入 git，`decision-auditor/` 运行时 gitignore）：
  - `APPEND_SYSTEM.md` — 唯一 agent 配置源（AGENTS.md/CLAUDE.md 蒸馏精简版，~4.7KB）
  - `extensions/memory.ts` + tsconfig — 每轮 `before_agent_start` 注入 .ai/ 摘要（最近 worklog 主题 + decisions/ 最新 ADR 标题 + inbox 列表，≤1.8KB，mtime 指纹缓存）
  - `agents/` — 5 个域专家（api/cli/web/deploy/flutter）转 pi-subagents 格式（`systemPromptMode: replace` + `inheritProjectContext: true` + `tools` allowlist）；MCP agent 不迁移（sunset）
  - `skills/` — remember-* 4 个 + memory-consolidate + image-recognition（vision.js + `.env` 从 .opencode 抢救）
  - `mcp.json` — notion MCP 迁移
- **`.ai/` 重构**：
  - decisions/ 评估后从 7 → 4：保留 mcp-sunset、production-cn-entry（原样）；blob、web-stack 精简（完整 blob 设计移 archive）；删 service-layer（已并入 APPEND_SYSTEM）、ai-agent-team（团队章程移 APPEND_SYSTEM）、cli-evaluation（D10/D12 已在 worklog）
  - inbox 7 个未消化文件消化完毕（沉淀 2 条：生产大文件上传超时坑 → worklog 08-08；人脸核身调研 → `requirements/2026-08-15-face-verification-auth.md`，🔶设计中）
  - worklog 新约定：一天一个 `YYYY-MM-DD.md`（`##` 按主题）；历史主题式文件保留不动
  - `README.md` 更新：自动注入（memory.ts）+ 自动捕获（context-mode，替代 capture-inbox hook）
- **`.gitignore`**：去掉旧工具条目，加 `.pi/decision-auditor/`、`.codegraph`

**验证**

- `.pi/extensions`：`bunx tsc --noEmit` 干净；dry-run mock pi 环境 → 注入摘要正确（worklog 主题 + decisions 内部标题）、缓存一致、非项目目录不注入
- 全量 `git status` 检查无脏文件；`.env`（image-recognition）确认未被提交

**坑 / 对下一次会话的提示**

- ⚠️ git index.lock 残留：lazygit 打开时会锁 index，`git add` 报错先 `rm -f .git/index.lock`
- ⚠️ pi 扩展 `before_agent_start` 的 `event.systemPromptOptions?.cwd` 是注入摘要的路径来源；扩展在 `.pi/extensions/` 自动发现，`/reload` 热加载
- 人脸核身需求（face-verification-auth）方向未定（替代 vs 兜底 passkey），后续会话需确认
- 剩余未迁移项目（其他 ~20 个）各有 .claude/.opencode 残留，按需逐个处理

## 人脸核身需求否决（2026-08-15 追加）

- `requirements/2026-08-15-face-verification-auth.md` 状态改为 🪦已否决：Passkey 平台生物识别（Face ID/Windows Hello）已免费覆盖需求，自托管要自己处理活体、云 API 每年几十元成本——投入产出不匹配，不推进。
