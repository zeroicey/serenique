# 2026-08-16

## 习惯模块（habit）全端实施

**需求**：记录「今天做了什么」的无压力流水账，不搞打卡/目标机制。四端全做：API / Web / CLI / AI。需求文档 `.ai/requirements/2026-08-16-habit-module.md`。

**数据模型**（2 表）：`habits`（name/kind good|bad/countable/sort_order）+ `habit_daily`（habit_id→cascade、date、status done|not_done|null、count、note，UNIQUE(habit_id,date)）。双模式：做没做型（跑步→✓/✗三态）+ 计数型（喝水→×N）。

**实施**（3 个子代理并行 + 主会话整合）：

- API Agent：`services/api/src/modules/habit/` 全套 8 文件 + 测试（32 pass 含真 PG 集成）+ db/schema.ts 注册 + app.ts 挂载 + exports.ts 导出 + audit 加 `habit.delete` 事件 + `ai.tools.ts` 加 6 工具（list/create/update/delete_habit、set_habit_daily、get_habit_overview）
- Web Agent：`apps/web/src/features/habit/`（api/queries/lib/schemas/components/pages 全套 + 48 测试）+ router 注册 /habit、/habit/overview + stores/habit-ui.ts
- CLI Agent：`apps/cli/internal/client/habit.go` + `cmd/habit.go`（9 子命令：list/create/update/delete/today/do/not/count/overview）+ root.go 注册 + habit_test.go（22 测试）
- 主会话整合：迁移 `drizzle/0016_typical_rocket_racer.sql`（script 包 TTY 生成 + db:migrate 应用）→ 起临时 PG 容器 `serenique-pg`（5433）→ HTTP 冒烟全过（建/标记/反例 400/daily/overview/级联删）

**契约对齐修复**（并行开发整合关键）：Web 端最初按文档假设 DailyEntry 含 `id/date/updatedAt`、overview 记录用 `habitName`，API 实际为 `{habitId,status,count,note}` 且用 `name` 字段 → 以 API 为准改 Web 7 文件（api.ts 类型、overview 组件 habitName→name + key 改 habitId、habit-row 删依赖 updatedAt 的死代码、4 个测试 mock 去多余字段）；CLI 侧结构天然一致。

**验证**：api typecheck + 32 测试（含集成）；web typecheck + vitest 292 全过；cli go test 全绿；lint 干净。

**坑 / 对下一次会话的提示**

- ⚠️ 跨端并行开发后必须做**契约对齐**：以 `services/api` 源码为准核对各端 entry 形状（本次 Web 端 `habitName` vs API `name`、多声明的 id/updatedAt 字段差点上线才暴露）
- ⚠️ web 测试必须 `bun run test`（vitest），`bun test` 会因 user-event 的 document 未定义大面积假失败（Web Agent 也踩了同一个坑，已修）
- ⚠️ 本地 5433 无 PG 时：`docker run -d --name serenique-pg -p 5433:5432 -e POSTGRES_USER=serenique -e POSTGRES_PASSWORD=serenique -e POSTGRES_DB=serenique postgres:16-alpine` 再 `db:migrate`
- ⚠️ `db:generate`/`db:migrate` 需 TTY：`script -q /dev/null bun run db:generate`
- ⚠️ 子代理模型（deepseek-v4-flash）高推理量请求会超时：interrupt→同角色 fallback 子代理续跑（保留已写文件）

## Flutter habit 模块 + 全端部署（2026-08-16 下午）

**Flutter**（flutter-agent）：`apps/mobile/lib/features/habit/`（habit_models/api/providers/page/overview_page/time + widgets/habit_date_nav、habit_tile、habit_edit_page）+ router.dart/app_shell.dart 注册入口 + test/features/habit/。验证：flutter analyze 无问题，flutter test 298 全过。契约对齐 API（DailyEntry 无 id/date/updatedAt，overview 记录用 name 字段）。

**部署**：

- push main（`a4dc1c9..d59de61`，2 commits：feat habit 全端 + docs）→ CI docker-publish 构建 `zeroicey/serenique-api:main`（digest b8845c3b）
- 生产 hpcore：迁移 0016（habits/habit_daily 表 + drizzle 记录 17 条）→ docker pull :main + tag latest + `docker compose up -d --force-recreate api` → 容器 healthy，/api/habits 路由在线（401 认证拦截为预期）
- Web Cloudflare：`VITE_API_BASE_URL=https://api.hcyj.xyz/serenique bun run build` + `bunx wrangler pages deploy dist --branch main` → pages.dev/custom 域名 /habit 均 200，env chunk 含 api.hcyj.xyz

**坑 / 对下一次会话的提示**

- ⚠️ CI 日志 grep 出的 digest 是 platform-specific image digest，不是 manifest list digest；用服务器 `docker inspect --format '{{.RepoDigests}}'` 的值去 CI 日志里 grep 验证是否为本次构建（b8845c3b 在日志出现 7 次即新镜像）
- ⚠️ flutter analyze 与 flutter test 并发跑会抢 startup lock（ephemeral/Packages 删除失败），串行跑

## 习惯模块备注重构（description）+ 日期导航移 header + 装机（2026-08-16 晚）

**用户澄清**：备注不是「每天记录带备注」，而是**习惯本身的简介 description**（habit 有 title + description）；同时要求 Web 日期选择器移到顶栏节省空间；Flutter 装机到手机。另确认数据模型**保持 habit_daily**（不做 habit_logs 事件流水——「没做」三态是 logs 天然短板，daily 一行两字段是最小统一模型）。

**4 子代理并行重构 + 整合**：

- API：habits 加 `description text`、habit_daily 删 `note`；迁移 0017（ALTER ADD/DROP）；types/domain/mappers/service/handler/AI 工具（create/update_habit 加 description、set_habit_daily 删 note）/system-prompt 同步
- Web：习惯行显示简介（单行 truncate）+ 弹窗加简介输入；**HabitDateNav 移入 header bar**（router.tsx `handle.headerRight`，仿 TaskGroupSwitcher），今天页主体只留列表
- CLI：create/update 加 --description；today 显示简介；do/not/count 删 --note
- Flutter：models/api 同步 description；tile 显示简介；edit 页加简介输入；删 note

**验证**：api typecheck + 30 测试（含真 PG）；web typecheck + vitest 293；cli go test；flutter analyze + 298 测试 全绿。

**部署**：4 commits（`79f8f3b..99460c3`）→ CI（digest f2cfc4fb）→ 生产迁移 0017（记录 18 条）+ 新镜像 healthy；Web Cloudflare /habit 200；iOS 装机（hcyj 入口，App installed ✓）。

**决策**：数据模型保持 habit_daily（D-006 延续），用户确认。

**坑 / 对下一次会话的提示**

- ⚠️ 「备注」语义歧义：先说清是习惯简介还是每日记录备注再动手，本次全端改了 description 又重部署一轮
- ⚠️ 跨端字段删除（note）后，注释里也会残留旧字段名，grep 时用 `grep -v 注释` 或全量清理

## 接入 Cloudflare 官方远程 MCP（4 server）

**背景**：项目重度使用 Cloudflare（Pages 部署 `serenique-web.pages.dev`、DNS 接管 `serenique.0icey.icu`、反代后取 `cf-connecting-ip` 做审计），此前 AI 只能靠手动 wrangler CLI + runbook，无法直接操作 CF 资源。

**改动**（commit `chore: add Cloudflare MCP servers config`）

- 新增仓库根 `.mcp.json`（随 git 提交），配置 4 个 **Cloudflare 官方远程 MCP server**（全部 `type: http`，OAuth 授权）：
  - `cloudflare-api` — `https://mcp.cloudflare.com/mcp`，Code Mode：`docs`（文档检索）/ `search`（OpenAPI 端点搜索）/ `execute`（代码执行全 API，~2500 端点）
  - `cloudflare-docs` — `https://docs.mcp.cloudflare.com/mcp`，文档检索 + Pages→Workers 迁移指南（免认证）
  - `cloudflare-observability` — `https://observability.mcp.cloudflare.com/mcp`，Workers 列表/代码/日志/指标分析（8 工具）
  - `cloudflare-dns` — `https://dns-analytics.mcp.cloudflare.com/mcp`，DNS 报告/zone 设置/记录查询（5 工具）
- 决策链补录：D-002（CF MCP 采纳）、D-003（人脸核身否决）

**验证**

- 4 个 URL curl 探测可达；`docs` 免认证 200，其余 3 个 401（预期，OAuth 后可用）
- OAuth 授权（`mcp({ action: "auth-start"/"auth-complete" })` 流程，浏览器授权后回传 `localhost:55981/callback?code=...` 完整 URL）逐个完成
- 冒烟：`cloudflare-dns` zones_list 成功读回账户 6 个 zone（`zeroicey.me` / `0icey.icu` / `162315.xyz` 等，Free 计划）
- 审查 workflow（reviewer，窗口 f418bd0d→411e97d）**无 blocker**；3 条 Note 均为低优先级（rate-limit 信任 XFF、timeout 504 被吞成 500、决策链完整性）已处理链补录

**坑 / 对下一次会话的提示**

- ⚠️ pi 在会话启动时读取 MCP 配置：改 `.mcp.json` 后必须**重启 pi** 才能看到新 server
- ⚠️ 远程 MCP OAuth 授权：`auth-start` 生成浏览器 URL → 授权后**复制地址栏 `localhost:55981/callback?code=...` 完整 URL** 回传 `auth-complete`（页面打不开没关系，只要 URL）
- ⚠️ token 存 **OS 钥匙串**（pi-mcp-adapter 用 `@napi-rs/keyring`，macOS Keychain），**不落盘 `.mcp.json`**（也不在 `~/.pi/agent/auth.json`——那个只放模型 provider 凭据），文件可安全提交 git
- ⚠️ 每个 server 单独 OAuth（docs 免认证，api/observability/dns 各一次）；Code Mode 权限范围在浏览器授权时勾选，操作类动作受其约束

## Moment 新建闪记本地草稿缓存（web + flutter，文字版）

**背景**：新建闪记正文在网络不好/误触返回/刷新/杀后台时丢失——Web 的 `useMomentDraftStore` 是纯内存会话态（注释明写），Flutter 的 `_controller` 纯内存，退出页面即丢。用户要求先做文字版本地缓存，附件不管。

**改动**（需求：`.ai/requirements/2026-08-16-moment-draft-cache.md`，✅已实施）

- **Web**：`stores/moment-draft.ts` 改用 zustand `persist` middleware（`name: 'serenique.moment.draft.text'`，`partialize` 只存 draftText）；`moment-create-page.tsx` 发布成功用 `reset({ text: '' })`（修复：`reset()` 回退到 defaultValues 快照——历史草稿会被 useEffect 写回 store）、**取消保留草稿**（误触不丢）
- **Flutter**：新增 `moment_draft_storage.dart`（`MomentDraftStorage` 抽象 + `SharedPrefsMomentDraftStorage` 实现 + provider，测试注入内存版）；`moment_create_page.dart` initState 恢复草稿、listener + 300ms 防抖写回（空串删除）、发布成功 `_clearSavedDraft()` 删除 + `_published` 标志防 dispose 回写、dispose fire-and-forget 保存防抖窗口内最后输入

**验证**：web 9 用例（store 3 + page 6）、flutter 13 用例（含新增 5 草稿用例）全过；web tsc / biome / flutter analyze 干净；moment 全模块 90 测试通过

**坑 / 对下一次会话的提示**

- ⚠️ Riverpod 3：`State.dispose()` 里**不允许用 `ref`**（会抛 “Using ref when a widget is about to be unmounted”），provider 引用要提前在 `initState` 缓存到字段（`late final _draftStorage = ref.read(...)`）
- ⚠️ flutter 测试中不注入 `SharedPreferences` mock 会挂起（`SharedPreferences.getInstance()` 走 method channel 无响应）→ 测试总注入内存 fake
- ⚠️ Web 发布成功必须 `reset({ text: '' })` 而非 `reset()`：后者回退到进入页面时的草稿快照，`useEffect` 会把旧草稿写回 store 造成残留
- ⚠️ 语义约定：**发布成功/删空文字才清草稿，取消/返回保留**（核心诉求是防误触丢失）

## Web 部署 Cloudflare + iOS 装机（hcyj 后端地址）+ push

**改动**（commit `chore: ignore wrangler cache, document hcyj entry for iOS install`）

- **Web 部署**：`apps/web` 构建（`VITE_API_BASE_URL=https://api.hcyj.xyz/serenique`）→ `bunx wrangler pages deploy dist --project-name=serenique-web --branch main`。⚠️ wrangler 命令超时（400s+）但**部署实际已成功**——CLI 输出超时≠失败，用 `wrangler pages deployment list` 核实：Production main 最新部署 = 当前 commit，线上 `index-*.js` 为新 chunk。
- **iOS 装机（hcyj 入口）**：`flutter build ios --release --dart-define=API_BASE_URL=https://api.hcyj.xyz/serenique`（D-005，用户明确后端地址写 hcyj 而非 zeroicey.me）+ `xcrun devicectl device install app`。验证：`strings build/ios/iphoneos/Runner.app/Frameworks/App.framework/App | grep hcyj` 应返回 URL；hcyj API 探活 401（可达，未带 token）。Dio baseUrl 支持路径前缀（`/serenique` 由 Caddy `handle_path` 剥离），与 Web 同入口。
- **gitignore**：加 `apps/web/.wrangler/`（wrangler 部署本地缓存，未被忽略过）。
- **push**：`5ef17b4..9271acf`（6 提交）→ origin/main。

**决策链**：D-005（iOS 装机后端入口切换 hcyj）。

**坑 / 对下一次会话的提示**

- ⚠️ wrangler pages deploy 命令超时≠部署失败：先 `wrangler pages deployment list` 看最新 Production 部署 commit，再 curl 线上验证，不要直接重跑
- ⚠️ iOS 装机默认走 hcyj 国内加速入口（runbook 已更新）；zeroicey.me 保留为备选。App 免费签名 7 天过期，过期需重签重装

## 结对审计修复轮（2026-08-16 深夜）

**来源**：自动 reviewer 完整跑完（未超时），无 blocker，但抓到 1 真 bug + 2 值得修：

- **Med-1（真 bug）**：HabitRow 本地乐观状态（localStatus/localCount）切换日期后不重置——切到另一天显示上一天虚假状态，再点会 clear 错误日期。修复：渲染期 derived state（prevDate 比较）重置。header 日期导航正是暴露面
- **Low-3**：AI set_habit_daily 直连 service 绕过 handler 的日期校验，非法日期可落库。修复：service.setDaily 加 DailyDateSchema 兜底（所有调用路径安全）；TypeBox RegEx 参数会破坏 execute 类型推断，改回 Type.String() + service 兜底
- **Med-2**：需求文档未同步 description 设计（note 残留、状态行旧）→ 已对齐 D-007
- Note-4/5（AI 路径绕过 zod transform、CLI --dec 与 Web 清除语义细微差）：既有模式，记录不修
- Note-6：工作树已确认干净

**验证**：web vitest 293 全过；api typecheck + habit/ai 42 pass（ai.integration 1 fail 为既有 mock 泄漏，stash 验证与本次改动无关）；部署 API（digest 精确拉取 6821f392，**又踩加速器缓存坑**）+ Web 全线上

**坑 / 对下一次会话的提示**

- ⚠️ 镜像加速器缓存 `:main` 旧 tag 第二次实测：`docker compose pull` 后 digest 仍是旧值 → 必须从 CI 日志 grep `containerimage` 行的 manifest digest 精确拉取（`docker pull <img>@sha256:<digest>`），不要相信 `docker pull :main` 的输出
- ⚠️ TypeBox `Type.RegEx` 会让 defineTool 的 execute 参数类型推断退化为 `{ [x: string]: any }`，破坏 service 调用类型检查——校验放 service 层兜底更稳
- ⚠️ web 测试必须 `bun run test`（vitest）：`bun test` 下 user-event 的 `document` 未定义大面积假失败（本次再次踩坑）

## 结对审计第二轮（reviewer 完整跑完，无 blocker）

**处理**：

- Low-3 需求文档 §5 Web 行残留「备注输入」→ 改为简介展示 + header 日期导航
- Low-4 补集成测试：`setDaily({date:'2026-02-30'}) → 400`（服务层日期兜底断言，14 集成测试全过）
- Low-1 setDaily 非原子 upsert（select-then-insert 并发双 insert → UNIQUE 冲突）：单用户低概率、无数据损坏，**记录不修**（Web 乐观状态已串行化）
- Low-2 双击竞态（setDaily/clearDaily 乱序）：乐观 UI 固有，低风险，**记录不修**
- Note-1 AI 路径绕过 zod transform（description:"" 落库空串）：既有模式，**记录不修**
- Note-2 countable 切换旧行统计失真：已接受权衡（需求 §3 声明）
- Note-3 state.json 并发写风险（多审计实例共享写）：审计机制问题，留意
- Note-4 工作树已确认干净
