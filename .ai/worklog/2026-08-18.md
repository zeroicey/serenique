# 2026-08-18

## AI 对话消息懒加载（后端分页 + Web + Flutter 全端）

**需求**：AI 对话会话变长后，`session_ready`/`session_switched` 一次性全量下发 + 前端全量渲染（每条 assistant 消息经 Streamdown/markdown 渲染）导致首屏慢、内存高。目标：初始只加载最新 N 条，向上滚动到顶部时再加载更早批次。

**方案（落盘 `.ai/requirements/2026-08-18-ai-message-lazy-load.md`）**：后端 `tailRenderMessages` 分页（全量转换后从尾部切片，天然保持 toolCall/toolResult 关联不断裂）+ WS 协议扩展（`load_more` c2s / `messages_loaded` s2c）+ `Conn.deliveredCount` per-connection 游标 + 前端 IntersectionObserver / ScrollController 触顶检测 + prepend 滚动位置补偿。

**后端（`services/api/src/modules/ai/`）**：

- `ai.service.ts`：新增 `tailRenderMessages(messages, limit, offset)` → `{ messages, total, hasMore }` + `INITIAL_PAGE_SIZE=20` / `MORE_PAGE_SIZE=30` 常量。`toRenderMessages` 全量纯函数不动（分页在 RenderMessage[] 层切片）
- `ai.types.ts`：`session_ready`/`session_switched` 增加 `totalMessageCount` + `hasMore`；新增 `load_more`（c2s，limit 可选）/ `messages_loaded`（s2c）
- `ai.handler.ts`：`Conn` 增加 `deliveredCount` 游标；`session_ready`/`session_switched`（`sessionPayload`）只发尾部 20 条 + 分页元信息；新增 `load_more` 分支按 `deliveredCount` 偏移取更早批次，累计 `deliveredCount`

**Web（`apps/web/src/features/ai/`）**：

- `lib/protocol.ts`：同步后端协议字段
- `store/ai-store.ts`：新增 `hasMoreMessages`/`loadingMore`/`totalMessages` + `loadMore()` action（防并发）；`messages_loaded` prepend；`session_ready`/`session_switched` 读新字段
- `components/message-list.tsx`：顶部哨兵 + `IntersectionObserver` 触发懒加载；`useLayoutEffect` 滚动锚定——通过比对 `messages[0]` 引用判断 prepend，prepend 时补偿 `scrollHeight` 增量，新消息/初始滚底；顶部「加载更早消息…」/「向上滚动加载更多」提示

**Flutter（`apps/mobile/lib/features/ai/`）**：

- `ai_protocol.dart`：新增 `ClientLoadMore`；`SessionReadyMessage`/`SessionSwitchedMessage` 增加 `totalMessageCount` + `hasMore`；新增 `MessagesLoadedMessage`
- `ai_controller.dart`：`AiState` 新增 `hasMoreMessages`/`loadingMore`/`totalMessages` + `loadMore()` action（防并发）；`MessagesLoaded` prepend；`SessionReady`/`SessionSwitched` 处理新字段
- `widgets/message_list.dart`：`ScrollController` 监听触顶（< 50px）触发 `loadMore`；`ref.listen` 检测 messages 头部增长（`prev.first != next.first`）判定 prepend，记录 `maxScrollExtent` 增量做位置补偿；顶部加载指示项（CircularProgressIndicator / "向上滚动加载更多"）

**测试**：

- 后端 `ai.service.test.ts`：`tailRenderMessages` 5 个分页用例（初始/offset/limit>total/空/turn 完整性）→ 8 pass
- 后端 `ai.handler.test.ts`：既有 4 用例不受影响 → 12 pass（含 service）
- Web `ai-store.test.ts`：`loadMore` 状态流转 + `messages_loaded` prepend + `hasMore` 边界 → 10 pass
- Web `message-list.test.tsx`：哨兵渲染 + 加载提示 → 7 pass
- Flutter `ai_protocol_test.dart`：`session_ready` 分页字段 + `messages_loaded` + `ClientLoadMore` 序列化 → 8 pass
- Flutter `ai_controller_test.dart`：`session_ready` hasMore + `loadMore` 防并发 + `messages_loaded` prepend → 13 pass
- Flutter `message_list_test.dart`：hasMore 提示 / loadingMore 指示器 / 无更多不渲染哨兵 → 7 pass
- 同步修复 `session_sheet_test`/`composer_bar_test`/`ai_page_test` 的 `AiState` 构造（新增 3 必填字段）
- **全绿**：后端 17 pass / Web 39 pass / Flutter 57 pass；全仓 typecheck ✓；flutter analyze ✓；biome（改动文件）✓

**提交**：1 commit（后端 + Web + Flutter + 需求文档）

**坑 / 对下一次会话的提示**

- ⚠️ 分页必须在 `RenderMessage[]` 层切片，不能在 `AgentMessage[]` 层——toolResult 不产生独立 RenderMessage（被关联进 assistant 的 toolCalls），从尾部切片天然保持 turn 完整性，在原始消息层切片会拆散 toolCall/toolResult
- ⚠️ 游标 per-connection（`Conn.deliveredCount`）：多标签页同会话各自独立分页互不干扰，切会话/建新会话必须重置（`sessionPayload` 每次赋值 `deliveredCount = tail.messages.length`）
- ⚠️ Web `useLayoutEffect` 滚动锚定合并成单个 effect，避免 prepend 补偿与新消息滚底两个 effect 互相打架导致抖动；通过 `messages[0]` 引用变化区分 prepend vs 尾部追加
- ⚠️ Flutter `ref.listen` 检测 prepend 比 Web 复杂：需比对 `prev.first != next.first`，且 postFrame 回调里读 `maxScrollExtent` 差值补偿，`_appendingOlder` flag 防止新消息滚底 effect 在 prepend 期间误触发
- ⚠️ 测试 JSON fixture 改协议字段后必须同步更新：Flutter `session_ready` 测试 JSON 没带 `totalMessageCount` 导致默认 0 vs 期望 1 失败；所有直接构造 `AiState` 的测试都要补 3 个新必填字段
- ⚠️ 既有 biome lint 报错（habit 模块 import 顺序）是历史遗留，非本次改动引入，本次改动文件单独 biome check 干净

## 修复（评审循环 2026-08-18，commit 前未提交）

对上述懒加载实现做了两轮 fresh-context 评审，修复 4 + 6 项缺陷：

### Round 1（评审 → 修复）

- 🔴 BLOCKER Web 懒加载形同虚设：`message-list.tsx` IntersectionObserver effect 依赖 `[loadMore]`（zustand 稳定 action），哨兵仅在 `hasMoreMessages=true` 时渲染，首次挂载 effect 提前 return 且永不重建 → **修复**：deps 加 `hasMoreMessages`，哨兵出现时重建 observer（新增 FIX1 回测：MockIntersectionObserver 验证「哨兵出现后才创建 + 进视口触发 loadMore」）
- 🔴 游标重复消息缺陷：原设计 `deliveredCount` 按「从尾部回数的已下发条数」做 offset，turn 在尾部追加消息后 offset 按新的更大 total 重算 → 批次与客户端已有消息重叠 → UI 重复 RenderMessage → **修复**：游标改为稳定前端边界 `conn.anchor`（客户端最早持有下标），`nextOlderPage` 返回 `[anchor-limit, anchor)`，fresh 轮次只在尾部追加、旧下标永不移动 → 永不重叠（回归用例固化「turn 追加后 load_more 不重叠」）
- `loadingMore` 永久卡死：`load_more` 分支异常时后端回 error 而非 `messages_loaded`，两端 error 分支不复位 → **修复**：Web store + Flutter controller 的 error 分支加 `loadingMore: false`
- handler 层分页零覆盖：`ai.handler.test.ts` 只有 `isAllowedOrigin` → **修复**：把分页接线抽成纯函数 `sessionPagination`（基线 = 尾部 + anchor）供 handler 复用，service 层补状态机测试

### Round 2（评审 → 本轮修复）

- Web prepend 重挂载：`key={i}` 使 prepend 后全部已持有消息 remount，ThinkingBlock/ToolCard 展开态重置、Streamdown 重渲染 → **修复**：store 镜像后端 anchor 新增 `oldestHeldIndex`（session 事件 = total - messages.length，`messages_loaded` 前移 batch 长度，尾部追加不变），`key = oldestHeldIndex + i`（组件回测：prepend 后已展开思考块不折叠）
- `load_more` limit 未校验：limit<=0 返回空批次却 hasMore=true、anchor 不变 → web 无限空转 → **修复**：钳到 [1, 200]（`MAX_PAGE_SIZE`）
- Web connect() 失败 / onclose 不复位 `loadingMore`（离线窗口期「加载更早消息…」卡文案）→ **修复**：两处补 `loadingMore: false`
- error→loadingMore 复位无测试 → **修复**：Web store + Flutter controller 各补 1 个用例
- 需求文档/工作日志仍描述 `deliveredCount`，与实现漂移（⚠️ 记忆注入会误导后续会话）→ **修复**：需求文档翻「已实施」+ 游标语义改 anchor、工作日志追加本节、ADR 落盘 `.ai/decisions/2026-08-18-ai-pagination-front-anchor.md`

⚠️ 分页游标必须锚定**稳定前端边界**（客户端最早持有下标），不能按「从尾部回数的已下发条数」实现——后者在 turn 追加后按新的更大 total 重算 offset，会与客户端已有消息重叠产生重复 UI。

## AI 对话自动会话管理（需求评审定稿 + 后端实施 + 子代理运行时故障）

**需求**（用户 2026-08-18）：宁序前端改**单一对话流**（无会话侧边栏/列表/切换/删除 UI，像 ChatGPT 单聊天框）；后端自动管理会话：①间隔 ≥24h 自动新建会话（链到旧会话）；②上下文近满就地压缩；③手动 `/new` `/compact` 斜杠命令（对齐 Hermes）；④用户时间线 = 会话链合并流，边界插「已开启新会话」marker，向上滚动跨会话加载；压缩点插可展开摘要 marker。

**评审（fresh reviewer，实证基于 pi-coding-agent 0.84.1 源码）**：3 阻断 + 4 建议 + 3 缺口，全部并入需求定稿：

- B1 压缩折叠会话内消息前缀 → **压缩 = 分页基线重同步点**（compaction_end 后服务端重算合并流尾页+total+anchor 下发）
- B2 session_switched 全量替换与单一对话流冲突 → 协议区分**链延续**（chainContinuation=true 不重置时间线，只插 marker）vs 切换
- B3 多标签并发建链分叉 → **链尾注册表** getOrCreateChainHead promise 去重
- S1 链落盘用 SDK 原生 `options.parentSession`（SessionInfo.parentSessionPath 已暴露）；S2 24h 判定用末条 `.timestamp` 非 mtime；S3 compact 失败 catch；S4 RenderMessage.kind 区分 system/compaction
- G2 前端无 UI → `switch_session` 停用；G3 未知斜杠命令=本地提示不发送（已定）

**落盘**：需求 `.ai/requirements/2026-08-18-ai-auto-session-management.md`（评审定稿）；决策链 D-016~D-020；README 状态表已加行。

**后端实施（api-agent 完成，已主会话独立验证）**：4 文件改动（ai.service.ts +269 / ai.handler.ts +199 / ai.types.ts +41 / ai.service.test.ts +205）

- 链模型 + 链尾注册表（promise 去重）、合并流分页（tailRenderStream/nextOlderPageFromStream/streamPagination + 派生 marker 计入下标）、session_ready/switched 增 chainContinuation/reason、session_compacted 压缩重同步、compaction_start/end 转发、compact c2s（失败 catch）、24h 判定（isSessionIdle，连接恢复+prompt 双入口）、toRenderMessages 扩展 compactionSummary → kind:'compaction'、switch_session 停用
- **验证**：bun test ai 模块 39 pass / 3 skip（DB 门控）/ 0 fail；typecheck 干净；biome 干净。**未提交**。

**前端（web + flutter）未实施** ⚠️ 被环境故障阻塞：

- ⚠️ **坑（升级级）**：pi-subagents 0.51.0（2026-08-18 发布）新增 `runner.type: external-job` + 内置 `gpt-pro` profile，07-19 01:47 热更新安装 `gpt-pro.md` 后，**所有子代理（含 builtin worker）启动即报** `Agent 'gpt-pro' has invalid runner.type; expected 'pi' or 'external-cli'`——worker 生产校验仍只认 pi/external-cli（版本不一致）。api-agent 因在更新前启动而成功。
- 修复：**重启 pi**（或回退 pi-subagents 0.50.x）后继续。
- 重启后续派（任务书在主会话本轮对话中有完整文本，两端都要求「以 services/api/src/modules/ai/*.ts 源码为契约唯一真相」）：web-agent 改 apps/web/src/features/ai/（protocol.ts 镜像新协议、ai-store 实现 chainContinuation 不重置/session_compacted 替换摘要/compaction marker/斜杠命令拦截、message-list 渲染 system/compaction marker、测试）；flutter-agent 改 apps/mobile/lib/features/ai/（ai_protocol.dart/ai_controller.dart/message_list.dart 同语义 + 测试）。
- 前端完成后：派 fresh reviewer 独立验收 → 全链路验证（bun test + typecheck + biome + flutter analyze）→ 主会话统一提交（后端+前端+需求文档，1 commit，英文 message）。
