# 2026-08-22 — 全仓架构审计 + R2 签名删除（BLOCKER 修复）

## 全仓架构审计（父会话编排，2 轮评审）

用户发起全仓审计：4 个 fresh-context reviewer 并行（API 骨架契约 / Web 前端 / CLI 硬契约 / 跨端架构），发现 1 BLOCKER + 大量 FIX/OPTIONAL；修复后第 2 轮 3 个 reviewer 复核 diff，无 blocker。

### 审计结论亮点

- API 12 模块：模块骨架 11/12 符合（ai 缺 schema/domain/mappers 系 WS-only 豁免），统一响应/错误契约/单一 db/契约锚定（moment `text`、event 裸数组、exports.ts）全部 ✅
- Web：导航移除决策、front-anchor 分页、TanStack Query 纪律 ✅；唯一体系性偏离是跨 feature import（已修）
- CLI：8 条硬契约主干全部成立（约 200 单测锁定）；缺 location/ai 命令系刻意范围（文档依据）
- 跨端契约四方一致（moment text/event 字段/裸数组/信封）；MCP 冻结验证通过
- ⚠️ 发现并修复：**r2 生产下 blob 删除/清理/旧 multipart 仍从 API 容器发 S3 IO，违反 D-032**（生产删除挂起 + R2 孤儿对象积累）→ 用户决策走 Worker 网关签名删除

## R2 签名删除（BLOCKER 修复，用户拍板方案）

新增 `del:` 签名域（与 `v1:` 读 / `up:` 写 隔离）：API `signR2Delete` 纯本地 HMAC 签发，浏览器直发网关 DELETE；API 容器零 R2 网络保持。

- `blob.domain.ts`：`signR2Delete`（`del:${path}:${expires}` hex）+ 固定向量测试
- `infra/r2-gateway/gateway.js`：DELETE 分支（校验 e/s/过期/del: 签名 → `BUCKET.delete` 幂等 200）+ CORS 加 DELETE + **fail-closed 守卫**（漏配 secret 时 500，不把 "undefined" 当 HMAC 密钥——第 2 轮评审抓出）
- `blob.service.ts`：`delete()` 返回 `{ deleted, deleteUrls }`——r2 模式删库前签发原图+缩略图签名删除 URL；local 模式行为不变。`upload/getFile/getThumbnail/cleanupOrphanFiles` r2 模式全部守卫（容器内零 S3 IO）
- `blob.handler.ts`：r2 返回 200+data，local 保持 204；Web 端 deleteBlob 双形态守卫 + 网关白名单 fire-and-forget 直删
- 遗留（后续任务）：CLI/mobile 未消费 deleteUrls（孤儿对象）；CLI 未接 R2 直传

## 附带修复（第 1 轮 FIX 批次）

- API：429 补 `code('RATE_LIMITED')`；删 `void logger` 死引用；根路由补 ai/ai-memory
- CLI：`snippet()` rune 安全截断；blob 上传/下载进度切 stderr（结果留 stdout）
- Web：location↔moment 双向依赖消除（`lib/location.ts` 承载类型+纯函数）；settings→auth 注释豁免
- 配置/文档：根 package.json 移除 mcp typecheck/test（冻结精神）；requirements 状态表同步；日记文档标已否决/横幅；Dockerfile/action 注释清理；`.pi/APPEND_SYSTEM.md`、release-process、`.env.example`、cli arch doc diary.go 残留同步（第 2 轮）

## 验证

API bun test 303/0（+164 skip 集成门控）、web vitest 352/0、CLI go vet + go test 4 包全绿、biome 380 文件无问题、node --check gateway.js、root typecheck 全绿。⚠️ web 测试要用 `vitest run`（bun test 会因缺 vitest 全局假失败，与改动无关）。

## 后续

- mobile（Flutter）`BlobApi.delete` 接入 deleteUrls → 直发网关 DELETE（独立任务）
- CLI blob upload 接 upload-url/confirm 直传（独立任务）
- 网关 DELETE 分支需 `wrangler deploy` 发布（secret 不变）；孤儿清理需本机对账脚本（runbook 已记提示）
- diff 未提交（38 文件 +314/-152），待用户 review 后按 Conventional Commits 提交

## 提交、部署与遗留事项解决（下午场）

用户指示「提交部署推送 + 解决遗留事项」，全部完成：

**提交推送（3 个 commit，均已推 main）**：

- `49fee45` fix(blob): R2 signed-delete（13 文件）
- `bebaa08` chore(audit): 契约/文档/mcp 清理（27 文件）
- `2c7d937` feat(cli): R2 direct upload; fix(mobile): deleteUrls（12 文件，+755）

**部署（hpcore）**：

- ⚠️ 坑：compose 引用 `:latest`，但 push main 只更新 Docker Hub `:main`（latest 仅在版本 tag 时打）→ `docker compose pull` 拉到旧镜像不重建。正确姿势：`docker pull zeroicey/serenique-api:main` → `docker tag ... :latest` → `compose up -d`（runbook 已记，本次又踩一次）
- 新容器 created 2026-08-22T16:33、healthy、日志「存储后端：Cloudflare R2」、/health OK
- 网关：`wrangler deploy`（走本地代理 127.0.0.1:7897，whoami/deploy 均需代理）发布 del: DELETE 分支；线上验证 OPTIONS Allow-Methods 含 DELETE、无签名 GET/DELETE 均 403

**遗留事项解决**：

- CLI 直传：`internal/client/blob.go` 新增 `UploadFileSmart`（upload-url→签名 PUT→confirm 三步，后端自适应回退 multipart，流式 SHA-256，transferHTTPClient 超时）；3 个新测试
- Mobile deleteUrls：`BlobApi.delete` 返回 `BlobDeleteResult`，`BlobActions` origin 白名单 fire-and-forget 直发网关；blob 测试 28 全过
- 孤儿清理：`services/api/scripts/orphan-cleanup-r2.ts`（幂等 + dry-run，DB vs R2 对账）
- gitignore 补 `infra/r2-gateway/.wrangler/`

验证：CLI go vet+test 4 包全绿、Flutter analyze 干净 + blob 测试 28 过、root typecheck 全绿、API bun test 467 无失败。
