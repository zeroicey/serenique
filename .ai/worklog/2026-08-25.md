# 2026-08-25

## AI 换 hpcore NewAPI 网关（ox-alpha）+ 部署

- **代码**（`46a7085` + `6e1a1d0`，均已推 main 并构建镜像）：
  - 缺省模型 `newapi/deepseek-v4-flash` → `newapi/ox-alpha`；缺省 baseUrl → `http://hpcore.hpnet.internal:3005/v1`
  - 解析优先级反转：显式 `AI_API_KEY`/`AI_BASE_URL` 时 env 驱动 models.json 优先于开发机 `~/.pi/agent/models.json`（否则开发机改 env 不生效）
  - **模型目录改为纯 AI_MODEL 单模型驱动**：生成的 models.json 只含 `AI_MODEL` 这一个 id，元数据用保守默认值（`AI_CONTEXT_WINDOW`/`AI_MAX_TOKENS` 可覆盖）。换端点/换模型从此只改 .env，无需改代码（用户明确否决了 JSON/env 清单方案：只用一个模型）
  - 同步 `.env.example`/README/README.zh-CN；删除废弃的 `OPENCODE_API_KEY` 引用
- **部署**（跳板已换 hcyj，见 runbook）：
  - 生产 `.env` 补 `AI_MODEL/AI_BASE_URL/AI_API_KEY`（备份 .env.bak.20260825-*）；compose 加 `serenique-ai-config:/data/ai` 卷 + extra_hosts 钉网关域名 + NO_PROXY 扩展
  - 按 digest `sha256:1cc2c81d…` 精确拉取绕过加速器缓存，tag 成 latest 后 `--force-recreate`
- ⚠️ **`/data/ai` 权限坑**：容器 `/data` 是 root 属主，无卷时 `mkdir /data/ai` EACCES → isAiEnabled **静默 false**（无报错日志）。修复：独立 named volume + 一次性 `docker exec -u root chown 10001:10001 /data/ai`。之前生产从未配过 key 所以没踩过。
- ⚠️ **NO_PROXY 坑**：hpcore 网关域名不进 NO_PROXY 时流量绕 mihomo → 502。已在 compose 排除 + extra_hosts 钉 IP（容器解析不了宿主 tailscale MagicDNS）。
- **验证**：容器内冒烟 isAiEnabled=true、真实对话往返（ox-alpha 回「你好」）、models.json 只含 ox-alpha；/health 正常。

## SSH 跳板变更

- hpazure 弃用，改走 hcyj（tailscale 节点）：`ssh -J hcyj hpcore`。runbook 已更新。
