# Environment
.env

node_modules/

# Go build artifacts
apps/cli/bin/

# Vite build output
apps/web/dist/

# wrangler pages deploy 本地缓存（每次部署生成）
apps/web/.wrangler/

# pi 运行时状态（.pi/ 其余内容——APPEND_SYSTEM.md、extensions/、agents/、skills/、mcp.json——纳入版本管理）
.pi/decision-auditor/

# 遗留工具运行时（已迁移 pi，不再使用）
.omo/
.superpowers/
.worktrees/
.codegraph

# 本地密钥暂存（不入库；生产密钥只进服务器 .env）
.secrets/
