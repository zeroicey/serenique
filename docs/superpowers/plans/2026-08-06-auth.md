# 认证（Auth）实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给 Serenique 加认证：单一共享密钥 `AUTH_TOKEN` + Bearer 认证 + 无状态 HttpOnly 签名 Cookie，覆盖 API / Web / CLI / MCP 四端，生产 fail-closed。

**Architecture:** `services/api` 新增 `auth` 模块（domain 纯函数 / service 单例 / handler / router）与一个跨模块认证中间件。凭证 = 部署者配置的高熵 `AUTH_TOKEN`；浏览器经 `POST /api/auth/login` 换取 HMAC 签名 Cookie（无状态，不建表）；CLI/移动端直接用密钥做 Bearer。Web 端加登录页 + AuthGuard + 媒体改走签名链接；CLI 加 `auth` 命令组；MCP 的 streamable-http 加同密钥校验。

**Tech Stack:** Bun + Hono + Zod（API）、React 19 + TanStack Query + ky + react-router（Web）、Go + cobra（CLI）、@modelcontextprotocol/sdk（MCP）。

## Global Constraints

- 用户可见文案一律中文（与现有模块一致）。
- 模块骨架：`*.types.ts / *.domain.ts / *.service.ts / *.handler.ts / *.router.ts / index.ts`；service 为单例对象，纯业务规则进 domain（无 DB/IO import）。
- 路径别名 `@/*` → `./src/*`；统一响应用 `Res` builder；service 抛 `AppError`。
- 常量时间比对用 `node:crypto` 的 `timingSafeEqual`；密钥/签名绝不出现明文日志、绝不进 `exports.ts` 之外的任何输出。
- 测试双层：单元测试无 DB；集成测试 `RUN_DB_TESTS=1` 门控。测试里 `setTestEnv()` 必须放在动态 `await import()` 之前（`@/env` 每进程只解析一次）。
- 凭证强度：`AUTH_TOKEN` ≥32 字符。生产（`NODE_ENV=production`）缺 `AUTH_TOKEN` → 启动即抛错（fail-closed）；dev/test 未设置则跳过认证。
- CLI 硬契约：错误必须返回 error（非零退出）；结果走 stdout、进度/错误走 stderr；token 输出一律 `maskToken()`；`confirm()` 只用于破坏性操作。
- Web：所有请求走全局 `api`（ky）实例，统一 `credentials:'include'`；不把密钥/凭证写入 localStorage。
- 命令入口：API 测试在 `services/api` 跑 `bun test`；Web 在 `apps/web` 跑 `bun run test`（vitest）+ `bun run typecheck`；CLI 在 `apps/cli` 跑 `go build ./... && go vet ./... && go test -count=1 ./...`。

---

## Phase 1 — API 认证内核（services/api）

### Task 1: auth 域纯函数（Cookie 签名/验签、常量比对、登录节流）

**Files:**
- Create: `services/api/src/modules/auth/auth.domain.ts`
- Test: `services/api/src/modules/auth/auth.domain.test.ts`

**Interfaces:**
- Produces (later tasks rely on these exact names/types):
  - `SESSION_COOKIE_NAME: string`（`"serenique_session"`）
  - `DEFAULT_SESSION_TTL_SECONDS = 30*24*3600`
  - `LOGIN_THROTTLE_WINDOW_MS = 10*60_000`、`LOGIN_THROTTLE_MAX_ATTEMPTS = 5`
  - `secretsEqual(actual: string, expected: string): boolean`
  - `signSessionCookie(secret: string, expires: number): string` → `"<exp>.<base64url>"`；HMAC-SHA256(secret, `"serenique-session." + exp`)
  - `verifySessionCookie(secret, value, nowSec): { valid: true } | { valid: false; reason: "malformed"|"tampered"|"expired" }`
  - `buildSessionCookie(value, maxAgeSeconds, crossSite, secure): string`
  - `clearSessionCookie(crossSite, secure): string`
  - `ThrottleState = { count: number; resetAtMs: number }`
  - `throttleIsBlocked(state, nowMs): boolean`
  - `throttleRecordFailure(state, nowMs, windowMs?): ThrottleState`
  - `throttleShouldBlock(state, nowMs, max?): boolean`

- [ ] **Step 1: 写失败测试**

`auth.domain.test.ts` 完整内容：

```ts
import { describe, expect, test } from "bun:test";
import {
  buildSessionCookie,
  clearSessionCookie,
  secretsEqual,
  signSessionCookie,
  throttleRecordFailure,
  throttleShouldBlock,
  verifySessionCookie,
} from "./auth.domain";

const SECRET = "0123456789abcdef0123456789abcdef"; // ≥32 chars

describe("secretsEqual", () => {
  test("rejects different values and lengths", () => {
    expect(secretsEqual("abc", "abc")).toBe(true);
    expect(secretsEqual("abc", "abd")).toBe(false);
    expect(secretsEqual("abc", "abcd")).toBe(false);
  });
});

describe("signSessionCookie / verifySessionCookie", () => {
  const exp = 1_800_000_000;
  const cookie = signSessionCookie(SECRET, exp);

  test("round-trips and expires", () => {
    expect(verifySessionCookie(SECRET, cookie, exp - 1)).toEqual({ valid: true });
    expect(verifySessionCookie(SECRET, cookie, exp + 1)).toEqual({ valid: false, reason: "expired" });
    expect(cookie.split(".").length).toBe(2);
    expect(Number(cookie.split(".")[0])).toBe(exp);
  });

  test("rejects tampering, malformed values, wrong secret", () => {
    const [, sig] = cookie.split(".");
    expect(verifySessionCookie(SECRET, `${exp + 1}.${sig}`, 1)).toEqual({ valid: false, reason: "tampered" });
    expect(verifySessionCookie(SECRET, `${exp}.xxxx`, 1)).toEqual({ valid: false, reason: "tampered" });
    expect(verifySessionCookie(SECRET, "nosig", 1)).toEqual({ valid: false, reason: "malformed" });
    expect(verifySessionCookie("yyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyy", cookie, exp - 1)).toEqual({ valid: false, reason: "tampered" });
  });
});

describe("buildSessionCookie", () => {
  test("sets HttpOnly + SameSite flags by mode", () => {
    const prod = buildSessionCookie("v", 3600, true, true);
    expect(prod).toContain("HttpOnly");
    expect(prod).toContain("SameSite=None");
    expect(prod).toContain("Secure");
    expect(prod).toContain("Max-Age=3600");
    const dev = buildSessionCookie("v", 3600, false, false);
    expect(dev).toContain("SameSite=Lax");
    expect(dev).not.toContain("Secure");
    expect(clearSessionCookie(true, true)).toContain("Max-Age=0");
  });
});

describe("throttle", () => {
  test("blocks only after the cap within the window", () => {
    let s: ReturnType<typeof throttleRecordFailure> | undefined;
    const now = 1_000_000;
    for (let i = 0; i < 4; i++) s = throttleRecordFailure(s, now);
    expect(throttleShouldBlock(s, now)).toBe(false);
    s = throttleRecordFailure(s, now);
    expect(throttleShouldBlock(s, now)).toBe(true);
    expect(throttleShouldBlock(s, now + 11 * 60_000)).toBe(false); // 窗口过期
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd services/api && bun test src/modules/auth/auth.domain.test.ts`
Expected: FAIL — `Cannot find module './auth.domain'`（文件不存在）。

- [ ] **Step 3: 写实现**

`services/api/src/modules/auth/auth.domain.ts` 完整内容：

```ts
import { createHmac, timingSafeEqual } from "node:crypto";

// ---------------------------------------------------------------------------
// Auth domain — pure rules: session cookie signing/verification, constant-time
// credential compare, and login-throttle state transitions. No DB / IO imports.
// ---------------------------------------------------------------------------

export const SESSION_COOKIE_NAME = "serenique_session";
export const DEFAULT_SESSION_TTL_SECONDS = 30 * 24 * 3600; // 30 天
export const LOGIN_THROTTLE_WINDOW_MS = 10 * 60_000; // 10 分钟
export const LOGIN_THROTTLE_MAX_ATTEMPTS = 5;

const SESSION_PREFIX = "serenique-session.";

/** Constant-time string compare (mirrors blob.domain signaturesEqual). */
export function secretsEqual(actual: string, expected: string): boolean {
  const a = Buffer.from(actual);
  const b = Buffer.from(expected);
  return a.length === b.length && timingSafeEqual(a, b);
}

/** Sign a session cookie: "<exp>.<base64url(HMAC-SHA256(secret, prefix+exp))>". */
export function signSessionCookie(secret: string, expires: number): string {
  const sig = createHmac("sha256", secret)
    .update(`${SESSION_PREFIX}${expires}`)
    .digest("base64url");
  return `${expires}.${sig}`;
}

export type SessionVerifyResult =
  | { valid: true }
  | { valid: false; reason: "malformed" | "tampered" | "expired" };

/** Verify a session cookie value at a given unix-second clock. */
export function verifySessionCookie(
  secret: string,
  value: string,
  nowSec: number,
): SessionVerifyResult {
  const dot = value.indexOf(".");
  if (dot <= 0) return { valid: false, reason: "malformed" };
  const expires = Number(value.slice(0, dot));
  if (!Number.isInteger(expires) || expires <= 0) {
    return { valid: false, reason: "malformed" };
  }
  const signature = value.slice(dot + 1);
  if (!secretsEqual(signature, signSessionCookie(secret, expires))) {
    return { valid: false, reason: "tampered" };
  }
  if (expires < nowSec) return { valid: false, reason: "expired" };
  return { valid: true };
}

/**
 * Build a Set-Cookie header. crossSite=true 用于生产（pages.dev → api.zeroicey.me）
 * 需 SameSite=None；dev 走 Vite 代理（同源）用 SameSite=Lax 即可在 http 下生效。
 * secure=false 用于 dev（http），生产必须 true（https）。
 */
export function buildSessionCookie(
  value: string,
  maxAgeSeconds: number,
  crossSite: boolean,
  secure: boolean,
): string {
  const parts = [
    `${SESSION_COOKIE_NAME}=${value}`,
    "Path=/",
    "HttpOnly",
    crossSite ? "SameSite=None" : "SameSite=Lax",
    `Max-Age=${maxAgeSeconds}`,
  ];
  if (secure) parts.push("Secure");
  return parts.join("; ");
}

export function clearSessionCookie(crossSite: boolean, secure: boolean): string {
  return buildSessionCookie("", 0, crossSite, secure);
}

// ---- Login throttle (pure state transitions; state held in-memory at service) ----

export type ThrottleState = { count: number; resetAtMs: number };

export function throttleIsBlocked(
  state: ThrottleState | undefined,
  nowMs: number,
): boolean {
  if (!state) return false;
  return nowMs < state.resetAtMs;
}

/** Record one failed attempt; returns the new state (window restarts on expiry). */
export function throttleRecordFailure(
  state: ThrottleState | undefined,
  nowMs: number,
  windowMs = LOGIN_THROTTLE_WINDOW_MS,
): ThrottleState {
  if (!state || nowMs >= state.resetAtMs) {
    return { count: 1, resetAtMs: nowMs + windowMs };
  }
  return { count: state.count + 1, resetAtMs: state.resetAtMs };
}

/** True when the attempt counter has hit the cap within the window. */
export function throttleShouldBlock(
  state: ThrottleState | undefined,
  nowMs: number,
  max = LOGIN_THROTTLE_MAX_ATTEMPTS,
): boolean {
  return throttleIsBlocked(state, nowMs) && (state?.count ?? 0) >= max;
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd services/api && bun test src/modules/auth/auth.domain.test.ts`
Expected: PASS（9 个用例）。

- [ ] **Step 5: 提交**

```bash
cd /Users/zeroicey/workspace/projects/serenique
git add services/api/src/modules/auth/auth.domain.ts services/api/src/modules/auth/auth.domain.test.ts
git commit -m "feat(api): auth domain — signed session cookie + constant-time compare + login throttle"
```

---

### Task 2: auth 类型 + service 单例（凭证校验、会话签发、登录节流）

**Files:**
- Create: `services/api/src/modules/auth/auth.types.ts`
- Create: `services/api/src/modules/auth/auth.service.ts`
- Modify: `services/api/src/test/helpers.ts`（`setTestEnv` 增加 `AUTH_TOKEN`）
- Test: `services/api/src/modules/auth/auth.service.test.ts`

**Interfaces:**
- Consumes: Task 1 的 domain 函数。
- Produces:
  - `LoginSchema = z.object({ token: z.string().trim().min(1).max(200) })`
  - `LoginInput = z.input<typeof LoginSchema>`
  - `AuthStatusEntry = { authenticated: boolean }`
  - `LoginOutcome = "ok" | "rejected" | "throttled"`
  - `authService`：`isAuthEnabled(): boolean`、`authenticate(token): boolean`、`sessionTtlSeconds(): number`、`createSessionCookie(): string`、`verifySessionCookie(value): boolean`、`login(ip, token, delayMs?, nowMs?): Promise<LoginOutcome>`

- [ ] **Step 1: 先改测试辅助（setTestEnv 增加 AUTH_TOKEN）**

在 `services/api/src/test/helpers.ts`：
1. 顶部常量区新增 `export const TEST_AUTH_TOKEN = "test-auth-token-0123456789abcdef";`（≥32 字符）。
2. `setTestEnv` 的 opts 类型加 `AUTH_TOKEN?: string;`。
3. 函数体内（`BLOB_SIGNING_SECRET` 之后）加：
   `process.env.AUTH_TOKEN ??= opts.AUTH_TOKEN ?? TEST_AUTH_TOKEN;`

理由：`@/env` 每进程只解析一次，`setTestEnv` 必须**总是**写入 `AUTH_TOKEN`（如同 `BLOB_SIGNING_SECRET`），否则先解析的文件会让后续 auth 测试读到空 token。

- [ ] **Step 2: 写失败测试**

`auth.service.test.ts`：

```ts
import { describe, expect, test } from "bun:test";
import { setTestEnv, TEST_AUTH_TOKEN } from "@/test/helpers";

setTestEnv(); // 总是带默认 AUTH_TOKEN

describe("authService", () => {
  test("authenticate accepts the configured token, rejects others", async () => {
    const { authService } = await import("./auth.service");
    expect(authService.isAuthEnabled()).toBe(true);
    expect(authService.authenticate(TEST_AUTH_TOKEN)).toBe(true);
    expect(authService.authenticate("wrong")).toBe(false);
  });

  test("createSessionCookie round-trips through verifySessionCookie", async () => {
    const { authService } = await import("./auth.service");
    const cookie = authService.createSessionCookie();
    expect(authService.verifySessionCookie(cookie)).toBe(true);
    expect(authService.verifySessionCookie("garbage")).toBe(false);
  });

  test("login ok / rejected / throttled", async () => {
    const { authService } = await import("./auth.service");
    expect(await authService.login("1.2.3.4", TEST_AUTH_TOKEN, 0)).toBe("ok");
    for (let i = 0; i < 5; i++) {
      expect(await authService.login("5.6.7.8", "bad", 0)).toBe("rejected");
    }
    expect(await authService.login("5.6.7.8", TEST_AUTH_TOKEN, 0)).toBe("throttled");
  });
});
```

- [ ] **Step 3: 跑测试确认失败**

Run: `cd services/api && bun test src/modules/auth/auth.service.test.ts`
Expected: FAIL — `Cannot find module './auth.service'`。

- [ ] **Step 4: 写实现**

`auth.types.ts`：

```ts
import { z } from "zod";

export const LoginSchema = z.object({
  token: z.string().trim().min(1).max(200),
});

export type LoginInput = z.input<typeof LoginSchema>;

export type AuthStatusEntry = { authenticated: boolean };
export type LoginOutcome = "ok" | "rejected" | "throttled";
```

`auth.service.ts`：

```ts
import { env } from "@/env";
import {
  DEFAULT_SESSION_TTL_SECONDS,
  secretsEqual,
  signSessionCookie,
  throttleRecordFailure,
  throttleShouldBlock,
  verifySessionCookie,
  type ThrottleState,
} from "./auth.domain";
import type { LoginOutcome } from "./auth.types";

// ---------------------------------------------------------------------------
// Auth service — singleton over env. No DB: the "identity" is the shared
// AUTH_TOKEN secret and the session cookie is stateless (HMAC-signed).
// ---------------------------------------------------------------------------

export const authService = {
  /** True when a credential is configured; dev without one skips auth. */
  isAuthEnabled(): boolean {
    return env.AUTH_TOKEN !== undefined && env.AUTH_TOKEN !== "";
  },

  /** Constant-time check of the shared secret. */
  authenticate(token: string): boolean {
    if (!this.isAuthEnabled()) return true; // dev 无密钥模式：接受
    return secretsEqual(token, env.AUTH_TOKEN!);
  },

  sessionTtlSeconds(): number {
    return env.SESSION_TTL ?? DEFAULT_SESSION_TTL_SECONDS;
  },

  /** Mint a fresh stateless session cookie value. */
  createSessionCookie(): string {
    const expires = Math.floor(Date.now() / 1000) + this.sessionTtlSeconds();
    return signSessionCookie(env.AUTH_TOKEN!, expires);
  },

  verifySessionCookie(value: string): boolean {
    if (!this.isAuthEnabled()) return true;
    return verifySessionCookie(
      env.AUTH_TOKEN!,
      value,
      Math.floor(Date.now() / 1000),
    ).valid;
  },

  // ---- Login throttle (in-memory, single-process) ----
  _throttle: new Map<string, ThrottleState>(),

  /**
   * Attempt login. Returns "throttled" when ip is blocked, "ok" on success
   * (clears throttle), else "rejected" after recording a failure and sleeping
   * delayMs (slow path). delayMs/nowMs injectable so unit tests avoid real sleeps.
   */
  login(
    ip: string,
    token: string,
    delayMs = 500,
    nowMs = Date.now(),
  ): Promise<LoginOutcome> {
    const state = this._throttle.get(ip);
    if (throttleShouldBlock(state, nowMs)) {
      return Promise.resolve("throttled");
    }
    if (!this.authenticate(token)) {
      this._throttle.set(ip, throttleRecordFailure(state, nowMs));
      return new Promise((resolve) => setTimeout(() => resolve("rejected"), delayMs));
    }
    this._throttle.delete(ip);
    return Promise.resolve("ok");
  },
};
```

- [ ] **Step 5: 跑测试确认通过**

Run: `cd services/api && bun test src/modules/auth/auth.service.test.ts`
Expected: PASS（3 个用例）。同时跑 `cd services/api && bun test src/test/helpers* 2>/dev/null; bun test src/modules/moment/moment.service.test.ts` 确认既有测试不受影响。

- [ ] **Step 6: 提交**

```bash
cd /Users/zeroicey/workspace/projects/serenique
git add services/api/src/modules/auth/auth.types.ts services/api/src/modules/auth/auth.service.ts services/api/src/test/helpers.ts services/api/src/modules/auth/auth.service.test.ts
git commit -m "feat(api): auth service — credential check, session cookie issuance, login throttle"
```

---

### Task 3: 错误码 + auth handler/router/barrel

**Files:**
- Modify: `services/api/src/shared/errors.ts`
- Create: `services/api/src/modules/auth/auth.handler.ts`
- Create: `services/api/src/modules/auth/auth.router.ts`
- Create: `services/api/src/modules/auth/index.ts`

**Interfaces:**
- Consumes: Task 1 domain、Task 2 service/types。
- Produces:
  - `authHandler.login(c)` / `authHandler.logout(c)` / `authHandler.me(c)`
  - `authRouter`（POST `/auth/login`、POST `/auth/logout`、GET `/auth/me`）
  - barrel `index.ts` 导出 `authRouter` 与 `authMiddleware`（Task 5 才创建 middleware，先留占位导出会在 Task 5 补齐——**改为只导出 authRouter**，middleware 在 Task 5 加到 barrel）。
  - `ErrorCode.UNAUTHORIZED`、`ErrorCode.RATE_LIMITED`

- [ ] **Step 1: 改共享错误码**

`services/api/src/shared/errors.ts` 的 `ErrorCode` 对象追加两行：

```ts
export const ErrorCode = {
  NOT_FOUND: "NOT_FOUND",
  VALIDATION: "VALIDATION",
  FORBIDDEN: "FORBIDDEN",
  CONFLICT: "CONFLICT",
  INTERNAL: "INTERNAL",
  UNAUTHORIZED: "UNAUTHORIZED",
  RATE_LIMITED: "RATE_LIMITED",
} as const;
```

- [ ] **Step 2: 写 handler**

`auth.handler.ts`：

```ts
import type { Context } from "hono";
import { env } from "@/env";
import { handleError } from "@/shared/handler";
import { Res } from "@/shared/response";
import {
  buildSessionCookie,
  clearSessionCookie,
} from "./auth.domain";
import { authService } from "./auth.service";
import { LoginSchema } from "./auth.types";

/** 尽量取真实客户端 IP（先 Cloudflare，再转发链，最后兜底）。 */
function clientIp(c: Context): string {
  return (
    c.req.header("cf-connecting-ip") ??
    c.req.header("x-forwarded-for")?.split(",")[0]?.trim() ??
    "unknown"
  );
}

export const authHandler = {
  async login(c: Context) {
    try {
      const body = LoginSchema.parse(await c.req.json());
      const result = await authService.login(clientIp(c), body.token);
      if (result === "throttled") {
        return Res.error("尝试过于频繁，请稍后再试").status(429).build(c);
      }
      if (result === "rejected") {
        return Res.error("认证失败，密钥不正确").status(401).build(c);
      }
      const cookie = authService.createSessionCookie();
      const crossSite = env.NODE_ENV === "production";
      const secure = env.NODE_ENV !== "development";
      c.header(
        "Set-Cookie",
        buildSessionCookie(cookie, authService.sessionTtlSeconds(), crossSite, secure),
      );
      return Res.ok("登录成功", { authenticated: true }).build(c);
    } catch (e) {
      return handleError(e, c, "auth");
    }
  },

  async logout(c: Context) {
    const crossSite = env.NODE_ENV === "production";
    const secure = env.NODE_ENV !== "development";
    c.header("Set-Cookie", clearSessionCookie(crossSite, secure));
    return Res.ok("已退出登录", { authenticated: false }).build(c);
  },

  /** 能走到这里即已通过中间件认证，恒为 true。 */
  async me(c: Context) {
    return Res.ok("查询成功", { authenticated: true }).build(c);
  },
};
```

`auth.router.ts`：

```ts
import { Hono } from "hono";
import { authHandler } from "./auth.handler";

export const authRouter = new Hono()
  .post("/auth/login", authHandler.login)
  .post("/auth/logout", authHandler.logout)
  .get("/auth/me", authHandler.me);
```

`index.ts`：

```ts
export { authRouter } from "./auth.router";
```

- [ ] **Step 3: 验证类型**

Run: `cd services/api && bun run typecheck`
Expected: PASS（handler/router 类型正确）。

- [ ] **Step 4: 提交**

```bash
cd /Users/zeroicey/workspace/projects/serenique
git add services/api/src/shared/errors.ts services/api/src/modules/auth/auth.handler.ts services/api/src/modules/auth/auth.router.ts services/api/src/modules/auth/index.ts
git commit -m "feat(api): auth handler/router — login/logout/me + UNAUTHORIZED/RATE_LIMITED codes"
```

---

### Task 4: env + CORS + app 接线（路由挂载 + fail-closed）+ 更新 app.test

**Files:**
- Modify: `services/api/src/env.ts`
- Modify: `services/api/src/middleware/cors.ts`
- Modify: `services/api/src/app.ts`
- Modify: `services/api/src/app.test.ts`

**Interfaces:**
- Consumes: Task 3 的 `authRouter`、`ErrorCode`。
- Produces: `env.AUTH_TOKEN?: string`、`env.SESSION_TTL: number`；`createApp(env)` 在 `NODE_ENV=production && !env.AUTH_TOKEN` 时抛错。

- [ ] **Step 1: env schema**

`env.ts` 的 schema 在 `BLOB_SIGNING_SECRET` 后追加：

```ts
  AUTH_TOKEN: z.string().min(32).optional(),
  SESSION_TTL: z.coerce.number().int().positive().default(30 * 24 * 3600),
```

（`SESSION_TTL` 单位为秒，默认 30 天。）

- [ ] **Step 2: CORS 支持凭证 + Authorization 头**

`middleware/cors.ts` 改为：

```ts
import { cors as honoCors } from "hono/cors";

// ---------------------------------------------------------------------------
// CORS middleware — allow all origins in development, configure in production.
// 设置 CORS_ORIGIN 时（生产，如 https://serenique-web.pages.dev）启用凭证
// 模式（credentials:true）——带 Cookie 的跨域请求不允许 origin 为 "*"。
// ---------------------------------------------------------------------------

export function cors() {
  const origin = process.env.CORS_ORIGIN ?? "*";
  return honoCors({
    origin,
    credentials: origin !== "*",
    allowMethods: ["GET", "POST", "PUT", "DELETE", "OPTIONS"],
    allowHeaders: ["Content-Type", "Content-Disposition", "Authorization"],
    maxAge: 86400,
  });
}
```

- [ ] **Step 3: app.ts 挂路由 + fail-closed**

`app.ts` 顶部 import 区加：

```ts
import { authRouter } from "@/modules/auth";
```

`createApp(env)` 函数体最前面（`const app = new Hono()` 之前）加：

```ts
  // ---- 0. Fail-closed: 生产必须配置 AUTH_TOKEN，否则拒绝启动 --------------
  //    createApp(env) 的 env 来自 @/env（index.ts 同一次解析），生产缺失即崩溃。
  if (env.NODE_ENV === "production" && !env.AUTH_TOKEN) {
    throw new Error("生产环境必须配置 AUTH_TOKEN 才能启动（认证 fail-closed）");
  }
```

在 `app.use("*", logger)` 之后、`app.route("/api", diaryRouter)` 之前加：

```ts
  app.use("/api/*", authMiddleware); // Task 5 实现，先注释掉避免未定义引用
  app.route("/api", authRouter);
```

> 注意：Task 5 才会创建 `authMiddleware`。若先提交本任务，将上面 `app.use("/api/*", authMiddleware)` 一行**注释掉**并加 `// Task 5: authMiddleware 在此挂载`，Task 5 再打开。authRouter 本任务即挂载（登录接口无中间件时无碍）。

- [ ] **Step 4: 更新 app.test.ts 带上 Bearer**

`setTestEnv()` 现在总是写入 `AUTH_TOKEN`，所以 `app.test.ts` 的 `/api` 请求必须带凭证，否则中间件（Task 5 起）会 401。本任务先为 Task 5 做好准备：

1. `import { setTestEnv, TEST_AUTH_TOKEN } from "@/test/helpers";`
2. `makeApp()` 的 createApp env 对象追加 `AUTH_TOKEN: TEST_AUTH_TOKEN,`（与 @/env 一致，供 fail-closed 检查读到）。
3. 新增一个 helper 常量：`const AUTH = { Authorization: \`Bearer ${TEST_AUTH_TOKEN}\` };`
4. 现有 `/api/*` 请求（diaries、blobs、nope、uuid 用例）的 options 都加 `headers: { ...AUTH, "content-type": "application/json" }`（GET 用 `{ ...AUTH }`）。
5. 新增两个 DB-free 用例（本任务即可写，Task 5 中间件上线后生效）：

```ts
  test("unauthenticated /api request → 401", async () => {
    const app = await makeApp();
    const res = await app.request("/api/moments");
    expect(res.status).toBe(401);
  });

  test("login with wrong token → 401, login with correct token sets cookie", async () => {
    const app = await makeApp();
    const bad = await app.request("/api/auth/login", {
      method: "POST",
      headers: { "content-type": "application/json" },
      body: JSON.stringify({ token: "wrong-token" }),
    });
    expect(bad.status).toBe(401);

    const ok = await app.request("/api/auth/login", {
      method: "POST",
      headers: { "content-type": "application/json" },
      body: JSON.stringify({ token: TEST_AUTH_TOKEN }),
    });
    expect(ok.status).toBe(200);
    const setCookie = ok.headers.get("set-cookie") ?? "";
    expect(setCookie).toContain("serenique_session=");
    expect(setCookie).toContain("HttpOnly");
  });
```

> 本任务提交时中间件未挂载，这两个新用例会 FAIL（401 变 200、cookie 仍会设置）。**允许本任务结束时这两个用例 pending/fail**，Task 5 挂中间件后转绿。若不想带着红测试提交，可将这两个用例放到 Task 5 一起写——执行者自行选择，本计划两处都列了。

- [ ] **Step 5: 验证既有测试**

Run: `cd services/api && bun run typecheck && bun test`
Expected: typecheck PASS；既有 app.test 用例 PASS（除上面两个新增用例在中间件未上线时失败，其余全绿）。

- [ ] **Step 6: 提交**

```bash
cd /Users/zeroicey/workspace/projects/serenique
git add services/api/src/env.ts services/api/src/middleware/cors.ts services/api/src/app.ts services/api/src/app.test.ts
git commit -m "feat(api): env AUTH_TOKEN/SESSION_TTL, CORS credentials, wire authRouter + fail-closed"
```

---

### Task 5: auth 中间件 + 挂载 + 集成测试

**Files:**
- Create: `services/api/src/modules/auth/auth.middleware.ts`
- Modify: `services/api/src/modules/auth/index.ts`（导出 middleware）
- Modify: `services/api/src/app.ts`（打开 `app.use("/api/*", authMiddleware)`）
- Create: `services/api/src/modules/auth/auth.service.integration.test.ts`

**Interfaces:**
- Consumes: Task 1–4。
- Produces: `authMiddleware: MiddlewareHandler`；放行规则：`/api/auth/login`、`/api/auth/logout`、签名 blob 链接（`/api/blobs/:id/file` 带 `expires`+`signature` 查询参数）。

- [ ] **Step 1: 写失败集成测试**

`auth.service.integration.test.ts`：

```ts
import { describe, expect, test } from "bun:test";
import { setTestEnv, TEST_AUTH_TOKEN, RUN_DB_TESTS } from "@/test/helpers";

setTestEnv();

// 认证中间件集成测试：真实 HTTP 走 createApp（DB 门控，登录成功后的写路径用真库）。
describe.skipIf(!RUN_DB_TESTS)("auth middleware integration", () => {
  async function makeAuthedApp() {
    const { createApp } = await import("@/app");
    return createApp({
      DATABASE_URL: process.env.DATABASE_URL!,
      BLOB_ROOT: process.env.BLOB_ROOT!,
      BLOB_MAX_SIZE: 104857600,
      BLOB_SIGNING_SECRET: process.env.BLOB_SIGNING_SECRET!,
      AUTH_TOKEN: TEST_AUTH_TOKEN,
      PORT: 3000,
      NODE_ENV: "test",
    });
  }

  test("rejects no-credential and wrong-token requests with 401", async () => {
    const app = await makeAuthedApp();
    expect((await app.request("/api/diaries")).status).toBe(401);
    expect(
      (
        await app.request("/api/diaries", {
          headers: { authorization: "Bearer wrong-token" },
        })
      ).status,
    ).toBe(401);
  });

  test("login → cookie → authed read/write round-trip", async () => {
    const app = await makeAuthedApp();
    const login = await app.request("/api/auth/login", {
      method: "POST",
      headers: { "content-type": "application/json" },
      body: JSON.stringify({ token: TEST_AUTH_TOKEN }),
    });
    expect(login.status).toBe(200);
    const setCookie = login.headers.get("set-cookie") ?? "";
    const cookie = setCookie.split(";")[0];

    // 带 Cookie 建日记
    const created = await app.request("/api/diaries", {
      method: "POST",
      headers: {
        "content-type": "application/json",
        cookie,
      },
      body: JSON.stringify({ content: "auth-e2e", diaryDate: "2026-08-06" }),
    });
    expect(created.status).toBe(201);
    const body = await created.json();
    const id = body.data.id;

    // 带 Cookie 读回
    const got = await app.request(`/api/diaries/${id}`, { headers: { cookie } });
    expect(got.status).toBe(200);
    expect((await got.json()).data.content).toBe("auth-e2e");
  });

  test("Bearer works for non-browser clients", async () => {
    const app = await makeAuthedApp();
    const res = await app.request("/api/auth/me", {
      headers: { authorization: `Bearer ${TEST_AUTH_TOKEN}` },
    });
    expect(res.status).toBe(200);
    expect((await res.json()).data.authenticated).toBe(true);
  });

  test("blob file route: signed link passes, plain path requires auth", async () => {
    const app = await makeAuthedApp();
    // 无签名、无凭证 → 401（中间件拦截，不落 DB）
    expect((await app.request("/api/blobs/0198f6d0-9e7c-71d7-8214-2a0f7f5f2001/file")).status).toBe(401);
    // 带签名参数 → 放行到 handler，由 handler 校验（此处 blob 不存在会 404，而非 401）
    const signed = await app.request(
      "/api/blobs/0198f6d0-9e7c-71d7-8214-2a0f7f5f2001/file?expires=9999999999&signature=bad",
    );
    expect(signed.status).not.toBe(401);
  });
});
```

> `describe.skipIf`：bun:test 支持。`RUN_DB_TESTS` 为 false 时整组跳过。

- [ ] **Step 2: 跑测试确认失败**

Run: `cd services/api && RUN_DB_TESTS=1 bun test src/modules/auth/auth.service.integration.test.ts`
Expected: FAIL — 401 用例因中间件未挂载会 200（或路由不存在 404），`Cannot find './auth.middleware'`。

- [ ] **Step 3: 写中间件**

`auth.middleware.ts`：

```ts
import type { Context, Next } from "hono";
import { Res } from "@/shared/response";
import { SESSION_COOKIE_NAME } from "./auth.domain";
import { authService } from "./auth.service";

// ---------------------------------------------------------------------------
// Auth middleware — the gate for every /api/* route.
// 凭证：Authorization: Bearer <AUTH_TOKEN>（CLI/移动端/脚本）或 HttpOnly 会话
// Cookie（浏览器）。放行：/api/auth/login、/api/auth/logout、签名 blob 链接
// （/api/blobs/:id/file?expires=&signature=，交给 blob handler 自行校验）。
// dev 未配置 AUTH_TOKEN 时整体跳过（本地零摩擦）。
// ---------------------------------------------------------------------------

const BLOB_FILE_ROUTE = /^\/api\/blobs\/[^/]+\/file$/;

function isSignedBlobLink(c: Context): boolean {
  const q = c.req.query();
  return BLOB_FILE_ROUTE.test(c.req.path) && Boolean(q.expires && q.signature);
}

export async function authMiddleware(c: Context, next: Next) {
  if (!authService.isAuthEnabled()) return next();
  if (c.req.path === "/api/auth/login" || c.req.path === "/api/auth/logout") {
    return next();
  }
  if (isSignedBlobLink(c)) return next();

  const header = c.req.header("Authorization");
  if (header && header.startsWith("Bearer ")) {
    const token = header.slice("Bearer ".length).trim();
    if (token && authService.authenticate(token)) return next();
    return Res.unauthorized("未认证或登录已过期").build(c);
  }

  const rawCookie = c.req.header("Cookie") ?? "";
  const m = rawCookie.match(new RegExp(`(?:^|;\\s*)${SESSION_COOKIE_NAME}=([^;]*)`));
  const value = m?.[1];
  if (value && authService.verifySessionCookie(value)) return next();

  return Res.unauthorized("未认证或登录已过期").build(c);
}
```

`index.ts` 追加导出：`export { authMiddleware } from "./auth.middleware";`

`app.ts`：把 Task 4 注释掉的 `app.use("/api/*", authMiddleware)` 打开，并补 import：`import { authMiddleware } from "@/modules/auth/auth.middleware";`（若 barrel 已导出，可用 `import { authMiddleware } from "@/modules/auth";`）。

- [ ] **Step 4: 跑测试确认通过**

Run: `cd services/api && RUN_DB_TESTS=1 bun test src/modules/auth/auth.service.integration.test.ts`
Expected: PASS（4 个用例）。
再跑 `cd services/api && bun test` 确认 app.test 新增的 401 / login 用例转绿、既有用例全绿。

- [ ] **Step 5: 全量验证**

Run: `cd services/api && bun run typecheck && bun test`
Expected: 全绿。

- [ ] **Step 6: 提交**

```bash
cd /Users/zeroicey/workspace/projects/serenique
git add services/api/src/modules/auth/auth.middleware.ts services/api/src/modules/auth/index.ts services/api/src/app.ts services/api/src/modules/auth/auth.service.integration.test.ts services/api/src/app.test.ts
git commit -m "feat(api): auth middleware gate + integration tests"
```

---

## Phase 2 — Web 端（apps/web）

### Task 6: ky 全局凭证 + auth API 契约

**Files:**
- Modify: `apps/web/src/api/client.ts`
- Create: `apps/web/src/features/auth/api.ts`
- Test: `apps/web/src/features/auth/api.test.ts`

**Interfaces:**
- Produces:
  - `api` 实例带 `credentials: 'include'`
  - `login(token: string): Promise<AuthStatus>`
  - `logout(): Promise<AuthStatus>`
  - `fetchAuthStatus(): Promise<AuthStatus>`（401 → `{ authenticated: false }`，不抛错）

- [ ] **Step 1: 写失败测试**

`api.test.ts`（模仿 `features/blob/api.test.ts` 的 mock 风格）：

```ts
import { afterEach, describe, expect, it, vi } from 'vitest'
import { api, apiUrl } from '@/api/client'
import { fetchAuthStatus, login, logout } from './api'

vi.mock('@/api/client', () => ({
  api: { get: vi.fn(), post: vi.fn() },
  apiUrl: (path: string) => `/api/${path.replace(/^\/+/, '')}`,
}))

const mockedGet = vi.mocked(api.get)
const mockedPost = vi.mocked(api.post)

function envelope(data: unknown, success = true) {
  return new Response(JSON.stringify({ success, message: 'ok', data }), {
    status: success ? 200 : 401,
    headers: { 'Content-Type': 'application/json' },
  })
}

describe('auth api', () => {
  afterEach(() => vi.clearAllMocks())

  it('login posts the token to /api/auth/login', async () => {
    mockedPost.mockResolvedValue(envelope({ authenticated: true }))
    const result = await login('secret')
    expect(mockedPost).toHaveBeenCalledWith('/api/auth/login', { json: { token: 'secret' } })
    expect(result.authenticated).toBe(true)
  })

  it('logout posts to /api/auth/logout', async () => {
    mockedPost.mockResolvedValue(envelope({ authenticated: false }))
    const result = await logout()
    expect(mockedPost).toHaveBeenCalledWith('/api/auth/logout', undefined)
    expect(result.authenticated).toBe(false)
  })

  it('fetchAuthStatus maps a 401 to authenticated:false', async () => {
    mockedGet.mockResolvedValue(
      new Response(JSON.stringify({ success: false, message: '未认证或登录已过期' }), {
        status: 401,
        headers: { 'Content-Type': 'application/json' },
      }),
    )
    const result = await fetchAuthStatus()
    expect(result.authenticated).toBe(false)
  })

  it('fetchAuthStatus returns authenticated on 200', async () => {
    mockedGet.mockResolvedValue(envelope({ authenticated: true }))
    const result = await fetchAuthStatus()
    expect(result.authenticated).toBe(true)
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd apps/web && bun run test -- src/features/auth/api.test.ts`
Expected: FAIL — `Cannot find module './api'`。

- [ ] **Step 3: 写实现**

`apps/web/src/api/client.ts`：`ky.create({ timeout: 15_000 })` 改为：

```ts
export const api = ky.create({
  timeout: 15_000,
  // 认证 Cookie 跨站携带（生产 pages.dev → api.zeroicey.me）。
  credentials: 'include',
})
```

`apps/web/src/features/auth/api.ts`：

```ts
import { api, apiUrl } from '@/api/client'
import { unwrap } from '@/api/unwrap'
import type { ApiEnvelope } from '@/types/api'

// Auth 模块 API 契约。浏览器端凭证为 HttpOnly 会话 Cookie（credentials:include）。

export interface AuthStatus {
  authenticated: boolean
}

export async function login(token: string): Promise<AuthStatus> {
  const res = await api.post(apiUrl('auth/login'), { json: { token } })
  return unwrap<AuthStatus>(res)
}

export async function logout(): Promise<AuthStatus> {
  const res = await api.post(apiUrl('auth/logout'))
  return unwrap<AuthStatus>(res)
}

export async function fetchAuthStatus(): Promise<AuthStatus> {
  const res = await api.get(apiUrl('auth/me'))
  const body = (await res.json()) as ApiEnvelope<AuthStatus>
  if (!body.success) {
    // 401 即未登录：AuthGuard 用它决定跳到登录页，不是错误。
    if (res.status === 401) return { authenticated: false }
    throw new Error(body.message || '查询认证状态失败')
  }
  return body.data as AuthStatus
}
```

- [ ] **Step 4: 跑测试确认通过 + typecheck**

Run: `cd apps/web && bun run test -- src/features/auth/api.test.ts && bun run typecheck`
Expected: PASS + typecheck PASS。

- [ ] **Step 5: 提交**

```bash
cd /Users/zeroicey/workspace/projects/serenique
git add apps/web/src/api/client.ts apps/web/src/features/auth/api.ts apps/web/src/features/auth/api.test.ts
git commit -m "feat(web): auth api contract + credentials include"
```

---

### Task 7: auth 状态 + 登录页 + AuthGuard + 退出按钮 + 路由

**Files:**
- Create: `apps/web/src/features/auth/queries.ts`
- Create: `apps/web/src/features/auth/pages/login-page.tsx`
- Create: `apps/web/src/features/auth/components/auth-guard.tsx`
- Create: `apps/web/src/features/auth/components/logout-button.tsx`
- Modify: `apps/web/src/app/router.tsx`
- Modify: `apps/web/src/app/layout/app-navbar.tsx`

**Interfaces:**
- Consumes: Task 6 的 `login/logout/fetchAuthStatus`。
- Produces: `useAuthStatus()`、`useLogin()`、`useLogout()`；`/login` 路由；根路由包 `AuthGuard`；导航栏挂 `LogoutButton`。

- [ ] **Step 1: 写 queries + 登录页 + 守卫 + 退出按钮**

`queries.ts`（沿用 `features/moment/queries.ts` 风格）：

```ts
import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query'
import { toast } from 'sonner'
import { fetchAuthStatus, login, logout } from './api'

// Auth 状态：应用加载时探一次；登录/退出后 invalidate 触发重新探测。

export function useAuthStatus() {
  return useQuery({
    queryKey: ['auth-status'],
    queryFn: fetchAuthStatus,
    retry: false,
  })
}

export function useLogin() {
  const queryClient = useQueryClient()
  return useMutation({
    mutationFn: login,
    onSuccess: () => queryClient.invalidateQueries({ queryKey: ['auth-status'] }),
    onError: (error) => toast.error(error.message || '登录失败'),
  })
}

export function useLogout() {
  const queryClient = useQueryClient()
  return useMutation({
    mutationFn: logout,
    onSuccess: () => queryClient.invalidateQueries({ queryKey: ['auth-status'] }),
    onError: (error) => toast.error(error.message || '退出登录失败'),
  })
}
```

`pages/login-page.tsx`：

```tsx
import { useState, type FormEvent } from 'react'
import { useNavigate } from 'react-router'
import { Button } from '@/components/ui/button'
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card'
import { Input } from '@/components/ui/input'
import { Label } from '@/components/ui/label'
import { useLogin } from '../queries'

// 登录页：输入部署时配置的认证密钥（AUTH_TOKEN），换取 HttpOnly 会话 Cookie。
export default function LoginPage() {
  const navigate = useNavigate()
  const [token, setToken] = useState('')
  const login = useLogin()

  async function onSubmit(e: FormEvent) {
    e.preventDefault()
    if (!token.trim()) return
    try {
      await login.mutateAsync(token.trim())
      navigate('/', { replace: true })
    } catch {
      // 错误已由 useLogin 的 onError toast 呈现
    }
  }

  return (
    <div className="flex h-screen w-screen items-center justify-center px-4">
      <Card className="w-full max-w-sm">
        <CardHeader>
          <CardTitle>登录 Serenique</CardTitle>
          <CardDescription>输入部署时配置的认证密钥</CardDescription>
        </CardHeader>
        <CardContent>
          <form onSubmit={onSubmit} className="space-y-4">
            <div className="space-y-2">
              <Label htmlFor="token">认证密钥</Label>
              <Input
                id="token"
                type="password"
                value={token}
                onChange={(e) => setToken(e.target.value)}
                placeholder="AUTH_TOKEN"
                autoFocus
                autoComplete="current-password"
              />
            </div>
            <Button type="submit" className="w-full" disabled={login.isPending}>
              {login.isPending ? '登录中…' : '登录'}
            </Button>
          </form>
        </CardContent>
      </Card>
    </div>
  )
}
```

`components/auth-guard.tsx`：

```tsx
import { Navigate, Outlet } from 'react-router'
import { PageLoading } from '@/app/layout/page-loading'
import { useAuthStatus } from '../queries'

// 认证门：探一次登录态。未登录 → 跳 /login；已登录 → 渲染子路由。
export function AuthGuard() {
  const { data, isLoading } = useAuthStatus()
  if (isLoading) return <PageLoading />
  if (!data?.authenticated) return <Navigate to="/login" replace />
  return <Outlet />
}
```

`components/logout-button.tsx`：

```tsx
import { LogOut } from 'lucide-react'
import { Button } from '@/components/ui/button'
import { useLogout } from '../queries'

// 退出登录：清 Cookie（logout 接口），随后 auth-status 失效 → AuthGuard 跳登录页。
export function LogoutButton() {
  const logout = useLogout()
  return (
    <Button
      variant="ghost"
      size="icon"
      onClick={() => logout.mutate()}
      title="退出登录"
      aria-label="退出登录"
    >
      <LogOut className="h-4 w-4" />
    </Button>
  )
}
```

- [ ] **Step 2: 改路由**

`apps/web/src/app/router.tsx`：

1. import 区加 `import { AuthGuard } from '@/features/auth/components/auth-guard'`。
2. 在 `createBrowserRouter([` 数组最前面加一条：

```tsx
  { path: '/login', element: lazyPage(() => import('@/features/auth/pages/login-page')) },
```

3. 把 `{ path: '/', element: <AppLayout />, children: [...] }` 改为两层嵌套：

```tsx
  {
    path: '/',
    element: <AuthGuard />,
    children: [
      { element: <AppLayout />, children: [ /* 原有 children 原样保留 */ ] },
    ],
  },
```

- [ ] **Step 3: 导航栏挂退出按钮**

`apps/web/src/app/layout/app-navbar.tsx`：

1. import 区加 `import { LogoutButton } from '@/features/auth/components/logout-button'`。
2. 在 `<div className="flex-1">{nav}</div>` 之后加 `<LogoutButton />`。

- [ ] **Step 4: 验证**

Run: `cd apps/web && bun run typecheck`
Expected: PASS。手动冒烟可选：`bun run dev` 打开应用，未登录应跳 /login。

- [ ] **Step 5: 提交**

```bash
cd /Users/zeroicey/workspace/projects/serenique
git add apps/web/src/features/auth apps/web/src/app/router.tsx apps/web/src/app/layout/app-navbar.tsx
git commit -m "feat(web): auth guard + login page + logout button + routes"
```

---

### Task 8: 媒体改走签名链接（跨站 Cookie 下 `<img>` 不受第三方 Cookie 拦截）

**Files:**
- Create: `apps/web/src/features/blob/access.ts`
- Modify: `apps/web/src/features/moment/components/moment-attachment-grid.tsx`
- Test: `apps/web/src/features/blob/access.test.ts`

**Interfaces:**
- Consumes: `api/apiUrl`、`unwrap`。
- Produces:
  - `createBlobAccessLink(blobId: string): Promise<string>`（返回**绝对**签名 URL）
  - `resolveApiPath(path: string): string`（相对 `/api/...` → 绝对，供 client.ts 复用）
  - `useBlobAccessUrls(blobIds: string[]): UseQueryResult<Record<string, string>>`

背景：`fileUrl` 是无签名的相对路径 `/api/blobs/:id/file`。auth 后浏览器跨站 `<img>` 带 Cookie 会被第三方 Cookie 策略拦截，且生产 pages.dev 上相对路径本就 404。改走 `POST /api/blobs/:id/access-link` 拿签名链接（凭证在 query，不受 Cookie 策略影响），顺带修好生产图裂。

- [ ] **Step 1: 先加 resolveApiPath 到 client.ts**

`apps/web/src/api/client.ts` 追加：

```ts
// 把 API 相对路径（如 /api/blobs/x/file）解析为绝对 URL（供 <img src> 等场景）。
export function resolveApiPath(path: string): string {
  const base = env.apiBaseUrl.replace(/\/+$/, '')
  const origin = base || (typeof window !== 'undefined' ? window.location.origin : '')
  return `${origin}${path.startsWith('/') ? path : `/${path}`}`
}
```

- [ ] **Step 2: 写失败测试**

`access.test.ts`：

```ts
import { afterEach, describe, expect, it, vi } from 'vitest'
import { api } from '@/api/client'
import { resolveApiPath } from '@/api/client'
import { createBlobAccessLink } from './access'

vi.mock('@/api/client', () => ({
  api: { post: vi.fn() },
  apiUrl: (path: string) => `/api/${path.replace(/^\/+/, '')}`,
  resolveApiPath: (path: string) => `https://api.example.com${path}`,
}))

const mockedPost = vi.mocked(api.post)

describe('createBlobAccessLink', () => {
  afterEach(() => vi.clearAllMocks())

  it('posts to access-link and returns an absolute signed URL', async () => {
    mockedPost.mockResolvedValue(
      new Response(
        JSON.stringify({
          success: true,
          message: 'ok',
          data: {
            path: '/api/blobs/b1/file?expires=9999999999&signature=sig',
            expires: 9999999999,
            signature: 'sig',
          },
        }),
        { status: 200, headers: { 'Content-Type': 'application/json' } },
      ),
    )
    const url = await createBlobAccessLink('b1')
    expect(mockedPost).toHaveBeenCalledWith('/api/blobs/b1/access-link', {
      json: { expiresInSeconds: 3600 },
    })
    expect(url).toBe('https://api.example.com/api/blobs/b1/file?expires=9999999999&signature=sig')
  })

  it('resolveApiPath builds an absolute URL from a relative path', () => {
    expect(resolveApiPath('/api/blobs/b1/file')).toBe('https://api.example.com/api/blobs/b1/file')
  })
})
```

- [ ] **Step 3: 跑测试确认失败**

Run: `cd apps/web && bun run test -- src/features/blob/access.test.ts`
Expected: FAIL — `Cannot find module './access'`。

- [ ] **Step 4: 写实现**

`apps/web/src/features/blob/access.ts`：

```ts
import { useQuery, type UseQueryResult } from '@tanstack/react-query'
import { api, apiUrl, resolveApiPath } from '@/api/client'
import { unwrap } from '@/api/unwrap'

// 签名访问链接：凭证放 query（expires+signature），不受跨站第三方 Cookie 策略影响，
// 是跨站 <img>/<video> 加载媒体的正解。dev 未配 BLOB_SIGNING_SECRET 时回退直链。

interface BlobAccessLinkEntry {
  path: string
  expires: number
  signature: string
}

export async function createBlobAccessLink(blobId: string): Promise<string> {
  const res = await api.post(apiUrl(`blobs/${blobId}/access-link`), {
    json: { expiresInSeconds: 3600 },
  })
  const link = await unwrap<BlobAccessLinkEntry>(res)
  return resolveApiPath(link.path)
}

export function useBlobAccessUrls(
  blobIds: string[],
): UseQueryResult<Record<string, string>> {
  return useQuery({
    queryKey: ['blob-access-urls', blobIds],
    queryFn: async () => {
      const entries = await Promise.all(
        blobIds.map(async (id) => {
          try {
            return await createBlobAccessLink(id)
          } catch {
            // dev 未配签名密钥：回退无签名直链（此时 dev 一般无 auth 或同源代理）
            return resolveApiPath(`/api/blobs/${id}/file`)
          }
        }),
      )
      return Object.fromEntries(blobIds.map((id, i) => [id, entries[i]]))
    },
    enabled: blobIds.length > 0,
    staleTime: 5 * 60_000,
  })
}
```

`moment-attachment-grid.tsx` 改法：

1. import 区加 `import { useBlobAccessUrls } from '@/features/blob/access'`。
2. 组件内（渲染 `mediaFiles` / 平铺图之前）取所有附件 blobId 并拉签名 URL：

```tsx
const blobIds = attachments.map((a) => a.blob.id)
const { data: accessUrls } = useBlobAccessUrls(blobIds)
```

3. `mediaFiles` 构造时把 `url: a.blob.fileUrl` 改为 `url: accessUrls?.[a.blob.id] ?? a.blob.fileUrl`。
4. `AttachmentTile` 的 `<img src={blob.fileUrl}>` 改为接收 `src` prop：把平铺层 `AttachmentTile({ attachment, src: accessUrls?.[attachment.blob.id] ?? attachment.blob.fileUrl })` 传入，`<img src={src}>`。

（`accessUrls` 未就绪时回退 `fileUrl`，避免首帧空白。）

- [ ] **Step 5: 验证**

Run: `cd apps/web && bun run test -- src/features/blob/access.test.ts && bun run typecheck`
Expected: PASS + typecheck PASS。

- [ ] **Step 6: 提交**

```bash
cd /Users/zeroicey/workspace/projects/serenique
git add apps/web/src/features/blob/access.ts apps/web/src/features/blob/access.test.ts apps/web/src/api/client.ts apps/web/src/features/moment/components/moment-attachment-grid.tsx
git commit -m "feat(web): load media via signed blob access links (cross-site cookie safe)"
```

---

## Phase 3 — CLI 端（apps/cli）

### Task 9: auth 命令组（login / logout / me）+ 注册

**Files:**
- Create: `apps/cli/cmd/auth.go`
- Create: `apps/cli/cmd/auth_test.go`
- Modify: `apps/cli/cmd/root.go`（注册 `authCmd`）

**Interfaces:**
- Consumes: `config.Load/Save`、`client.NewClient`、`apiClient`（包级）、`commandContext`、`maskToken`、`printer`。
- Produces: `serenique auth login`（写密钥并验证连通）、`auth logout`（清密钥）、`auth me`（验状态）。`client.setHeaders` 已自动带 `Authorization: Bearer`，无需改 client。

- [ ] **Step 1: 写失败测试**

`auth_test.go`（沿用 `runWithServer`）：

```go
package cmd

import (
	"net/http"
	"os"
	"path/filepath"
	"strings"
	"testing"
)

// TestAuthLoginWritesToken 验证 auth login 用候选密钥通过 /api/auth/me 后把密钥写入配置。
func TestAuthLoginWritesToken(t *testing.T) {
	// 隔离配置目录，避免污染真实 ~/.serenique/config.yaml
	dir := t.TempDir()
	if err := os.MkdirAll(dir, 0o700); err != nil {
		t.Fatal(err)
	}
	config.SetPath(filepath.Join(dir, "config.yaml"))

	authLoginToken = "secret-token"
	runWithServer(t, func(w http.ResponseWriter, r *http.Request) {
		if r.URL.Path != "/api/auth/me" {
			t.Fatalf("unexpected path %q", r.URL.Path)
		}
		w.Header().Set("Content-Type", "application/json")
		w.Write([]byte(`{"success":true,"message":"ok","data":{"authenticated":true}}`))
	}, true, func(srv *httptest.Server) {
		if err := authLoginCmd.RunE(authLoginCmd, nil); err != nil {
			t.Fatal(err)
		}
	})

	cfg, err := config.Load()
	if err != nil {
		t.Fatal(err)
	}
	if cfg.Token != "secret-token" {
		t.Fatalf("token = %q, want %q", cfg.Token, "secret-token")
	}
}

// TestAuthLogoutClearsToken 验证 logout 清空配置里的密钥。
func TestAuthLogoutClearsToken(t *testing.T) {
	dir := t.TempDir()
	config.SetPath(filepath.Join(dir, "config.yaml"))

	cfg := config.Default()
	cfg.Token = "secret-token"
	if err := config.Save(cfg); err != nil {
		t.Fatal(err)
	}

	if err := authLogoutCmd.RunE(authLogoutCmd, nil); err != nil {
		t.Fatal(err)
	}

	loaded, err := config.Load()
	if err != nil {
		t.Fatal(err)
	}
	if loaded.Token != "" {
		t.Fatalf("token = %q, want empty", loaded.Token)
	}
}

// TestAuthLoginRejectsBadToken 验证密钥错误时返回 error（非零退出）。
func TestAuthLoginRejectsBadToken(t *testing.T) {
	dir := t.TempDir()
	config.SetPath(filepath.Join(dir, "config.yaml"))

	authLoginToken = "bad-token"
	runWithServer(t, func(w http.ResponseWriter, r *http.Request) {
		w.Header().Set("Content-Type", "application/json")
		w.WriteHeader(http.StatusUnauthorized)
		w.Write([]byte(`{"success":false,"message":"未认证或登录已过期"}`))
	}, true, func(srv *httptest.Server) {
		if err := authLoginCmd.RunE(authLoginCmd, nil); err == nil {
			t.Fatal("expected an error for a rejected token")
		}
	})
}
```

（`runWithServer` 在 `commands_test.go` 定义，同包可直接用；`config.SetPath` 来自 config 包。）

- [ ] **Step 2: 跑测试确认失败**

Run: `cd apps/cli && go test -count=1 ./cmd/ -run TestAuth`
Expected: FAIL — `undefined: authLoginCmd`。

- [ ] **Step 3: 写实现**

`auth.go`：

```go
package cmd

import (
	"bufio"
	"errors"
	"fmt"
	"os"
	"strings"

	"github.com/spf13/cobra"
	"github.com/zeroicey/serenique-cli/internal/client"
	"github.com/zeroicey/serenique-cli/internal/config"
)

// AuthStatus 匹配 API 的 GET /api/auth/me 响应 data。
type AuthStatus struct {
	Authenticated bool `json:"authenticated"`
}

// authCmd 是 auth 命令组的父命令。
var authCmd = &cobra.Command{
	Use:   "auth",
	Short: "认证管理",
	Long:  "配置、验证 Serenique 的认证密钥（部署时在服务端 .env 配置的 AUTH_TOKEN）。",
	Args:  cobra.NoArgs,
}

// auth login
var authLoginCmd = &cobra.Command{
	Use:   "login",
	Short: "配置认证密钥",
	Long: `将认证密钥写入配置文件并验证连通性。

示例:
  serenique auth login
  serenique auth login --token <secret>   # 非交互式（供脚本/AI 使用）`,
	Args: cobra.NoArgs,
	RunE: func(cmd *cobra.Command, args []string) error {
		token := strings.TrimSpace(authLoginToken)
		if token == "" {
			fmt.Fprint(os.Stderr, "请输入认证密钥: ")
			line, err := bufio.NewReader(os.Stdin).ReadString('\n')
			if err != nil {
				return fmt.Errorf("读取输入失败: %w", err)
			}
			token = strings.TrimSpace(line)
		}
		if token == "" {
			return errors.New("认证密钥不能为空")
		}

		// 用候选密钥验证连通性（沿用当前 baseurl）
		probe, err := client.NewClient(apiClient.BaseURL, token)
		if err != nil {
			return err
		}
		var status AuthStatus
		if err := probe.Get(commandContext(cmd), "/api/auth/me", nil, &status); err != nil {
			return err
		}

		cfg, err := config.Load()
		if err != nil {
			return err
		}
		cfg.Token = token
		if err := config.Save(cfg); err != nil {
			return err
		}

		if useJSON {
			printer.PrintSuccess("认证密钥已保存", map[string]any{"token": maskToken(token)})
			return nil
		}
		printer.PrintMessage("✓ 认证密钥已保存到配置文件")
		return nil
	},
}

var authLoginToken string

// auth logout
var authLogoutCmd = &cobra.Command{
	Use:   "logout",
	Short: "清除认证密钥",
	Long:  "从配置文件中清除已保存的认证密钥。",
	Args:  cobra.NoArgs,
	RunE: func(cmd *cobra.Command, args []string) error {
		cfg, err := config.Load()
		if err != nil {
			return err
		}
		cfg.Token = ""
		if err := config.Save(cfg); err != nil {
			return err
		}
		if useJSON {
			printer.PrintSuccess("已清除认证密钥", map[string]any{"token": ""})
			return nil
		}
		printer.PrintMessage("✓ 已清除认证密钥")
		return nil
	},
}

// auth me
var authMeCmd = &cobra.Command{
	Use:   "me",
	Short: "验证认证状态",
	Long:  "调用 GET /api/auth/me 验证当前认证密钥是否有效。",
	Args:  cobra.NoArgs,
	RunE: func(cmd *cobra.Command, args []string) error {
		var status AuthStatus
		if err := apiClient.Get(commandContext(cmd), "/api/auth/me", nil, &status); err != nil {
			return err
		}
		if useJSON {
			printer.PrintSuccess("认证状态", map[string]any{"authenticated": status.Authenticated})
			return nil
		}
		printer.PrintKeyValue(map[string]string{"认证状态": fmt.Sprintf("%v", status.Authenticated)})
		return nil
	},
}

func init() {
	authLoginCmd.Flags().StringVar(&authLoginToken, "token", "", "认证密钥（省略则交互式输入）")
	authCmd.AddCommand(authLoginCmd)
	authCmd.AddCommand(authLogoutCmd)
	authCmd.AddCommand(authMeCmd)
}
```

`root.go` 的 `init()` 里、`rootCmd.AddCommand(eventCmd)` 后加一行：

```go
	rootCmd.AddCommand(authCmd)
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd apps/cli && go build ./... && go vet ./... && go test -count=1 ./...`
Expected: 全绿（auth 3 个用例 + 既有全部）。

- [ ] **Step 5: 提交**

```bash
cd /Users/zeroicey/workspace/projects/serenique
git add apps/cli/cmd/auth.go apps/cli/cmd/auth_test.go apps/cli/cmd/root.go
git commit -m "feat(cli): auth login/logout/me commands"
```

---

## Phase 4 — MCP 端（services/mcp）

### Task 10: MCP streamable-http 传输加 Bearer 校验

**Files:**
- Modify: `services/mcp/src/app.ts`
- Modify: `services/mcp/src/app.test.ts`

**Interfaces:**
- Consumes: `process.env.AUTH_TOKEN`（与 API 同密钥；读取发生在 createMcpApp 调用时，避免 env 缓存问题）。
- Produces: `/mcp` 及非 `/health`、非 `/` 路由要求 `Authorization: Bearer <AUTH_TOKEN>`（常量时间比对）；未配置 `AUTH_TOKEN` 时保持现状（内网）。

- [ ] **Step 1: 写失败测试**

`app.test.ts` 顶部 `setTestEnv()` 函数体追加一行（保证有测试密钥）：

```ts
  process.env.AUTH_TOKEN ??= "test-mcp-token-0123456789abcdef";
```

在 `describe("createMcpApp", ...)` 内新增第一个用例（放最前，保证先于其他用例设置 token）：

```ts
  test("requires a bearer token on /mcp when AUTH_TOKEN is set", async () => {
    setTestEnv();
    const { createMcpApp } = await import("./app");
    const app = createMcpApp({ enableJsonResponse: true });

    const noAuth = await app.request("/mcp", {
      method: "POST",
      headers: { "content-type": "application/json" },
      body: JSON.stringify({ jsonrpc: "2.0", id: 1, method: "initialize", params: {} }),
    });
    expect(noAuth.status).toBe(401);

    const withAuth = await app.request("/mcp", {
      method: "POST",
      headers: {
        "content-type": "application/json",
        authorization: "Bearer test-mcp-token-0123456789abcdef",
      },
      body: JSON.stringify({ jsonrpc: "2.0", id: 1, method: "initialize", params: {} }),
    });
    expect(withAuth.status).not.toBe(401);
  });
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd services/mcp && bun test`
Expected: FAIL — 新用例收到 200（未拦截），返回非 401。

- [ ] **Step 3: 写实现**

`services/mcp/src/app.ts`：

1. import 区加 `import { timingSafeEqual } from "node:crypto";`
2. 加一个常量时间比对 helper（模块级）：

```ts
/** 常量时间比对，避免时序侧信道。 */
function safeEqual(actual: string, expected: string): boolean {
  const a = Buffer.from(actual);
  const b = Buffer.from(expected);
  return a.length === b.length && timingSafeEqual(a, b);
}
```

3. 在 `app.all("/mcp", ...)` 之前加认证中间件（放行 `/health`、`/`）：

```ts
  // 传输认证：与 API 共享同一 AUTH_TOKEN。未配置时保持现状（内网）。
  const mcpAuthToken = process.env.AUTH_TOKEN;
  app.use("*", async (c, next) => {
    if (!mcpAuthToken) return next();
    if (c.req.path === "/health" || c.req.path === "/") return next();
    const header = c.req.header("Authorization");
    if (header && header.startsWith("Bearer ") && safeEqual(header.slice("Bearer ".length).trim(), mcpAuthToken)) {
      return next();
    }
    return c.json(
      { jsonrpc: "2.0", error: { code: -32600, message: "未认证或登录已过期" }, id: null },
      401,
    );
  });
```

（`mcpAuthToken` 在 createMcpApp 调用时捕获，测试逐例可控。）

- [ ] **Step 4: 跑测试确认通过**

Run: `cd services/mcp && bun test`
Expected: 新用例 PASS，既有用例（不带 token 但 MCP client 用 http 连接）——注意：既有工具用例用 `StreamableHTTPClientTransport` 连接 `/mcp`，现在带 token 的测试若先于它们把 `process.env.AUTH_TOKEN` 写死，会让它们也要求 token。若既有用例失败，给 `StreamableHTTPClientTransport` 构造传入 `customHeaders: { Authorization: "Bearer test-mcp-token-0123456789abcdef" }`（SDK 支持），保持全绿。

- [ ] **Step 5: 提交**

```bash
cd /Users/zeroicey/workspace/projects/serenique
git add services/mcp/src/app.ts services/mcp/src/app.test.ts
git commit -m "feat(mcp): require shared bearer token on streamable-http transport"
```

---

## Phase 5 — 部署与文档

### Task 11: 部署配置 + 文档 + worklog

**Files:**
- Modify: `.env.example`
- Modify: `docker-compose.yml`
- Modify: `CLAUDE.md`（env/路由表补充 auth 行）
- Create: `.ai/worklog/2026-08-06-auth-implementation.md`

- [ ] **Step 1: .env.example 追加**

在 `BLOB_SIGNING_SECRET` 段落之后加：

```bash
# 认证：所有端共用的高熵密钥（≥32 字符，建议 48+）。生产缺失则 API 拒绝启动。
# 换掉它并重启 = 全端（Web 会话 + CLI + MCP）立即失效。
AUTH_TOKEN=change-me-change-me-change-me-change-me

# Web 会话 Cookie 有效期（秒），默认 30 天。
# SESSION_TTL=2592000
```

- [ ] **Step 2: docker-compose.yml 追加环境变量**

`api` 服务 environment 里（`BLOB_SIGNING_SECRET` 后）加：

```yaml
      # 认证：生产 fail-closed（缺 AUTH_TOKEN 启动即拒）。
      AUTH_TOKEN: ${AUTH_TOKEN:?Set AUTH_TOKEN in the project root .env}
      SESSION_TTL: ${SESSION_TTL:-2592000}
      CORS_ORIGIN: ${CORS_ORIGIN:-}
```

`mcp` 服务 environment 里加：

```yaml
      AUTH_TOKEN: ${AUTH_TOKEN:?Set AUTH_TOKEN in the project root .env}
```

> 注意：`AUTH_TOKEN: ${AUTH_TOKEN:?}` 会强制要求 .env 有值——这是**故意的**（fail-closed）。若想本地不带认证跑 compose，可改成 `${AUTH_TOKEN:-}`；生产必须带值。

- [ ] **Step 3: CLAUDE.md 更新**

1. `services/api` 的 env 行（`BLOB_SIGNING_SECRET` 后）补 `AUTH_TOKEN`、`SESSION_TTL`。
2. API 路由表新增三行：

```markdown
| POST | `/api/auth/login` | 认证登录（密钥换 HttpOnly 会话 Cookie） |
| POST | `/api/auth/logout` | 退出登录（清 Cookie） |
| GET | `/api/auth/me` | 登录态查询 |
```

3. 新增一节「认证（Auth）」简述：单一共享密钥 `AUTH_TOKEN`（≥32 字符，生产 fail-closed）；CLI/移动端用 Bearer，Web 走登录表单换 HttpOnly Cookie（无状态 HMAC 签名，无会话表）；放行 `/health`、`/`、`/api/auth/login`、签名 blob 链接；换密钥 = 全端失效。

- [ ] **Step 4: 写 worklog**

`.ai/worklog/2026-08-06-auth-implementation.md`：记录实现要点、测试结果（`services/api` bun test 全绿、`apps/web` vitest+typecheck 全绿、`apps/cli` go build/vet/test 全绿、`services/mcp` bun test 全绿）、部署步骤（配 `AUTH_TOKEN` + `BLOB_SIGNING_SECRET` + `CORS_ORIGIN` 再 `docker compose up -d --build api mcp`）、以及「对下一次会话的提示」：
- 生产 `AUTH_TOKEN` 缺失会启动失败（fail-closed），先配再重启。
- `CORS_ORIGIN` 必须设为 Web 域名（带凭证跨域不允许 `*`）。
- 媒体加载依赖 `BLOB_SIGNING_SECRET`（Web 改走签名链接）；dev 未配时回退直链。
- 换密钥 = 全端失效，无会话表可清。

- [ ] **Step 5: 提交**

```bash
cd /Users/zeroicey/workspace/projects/serenique
git add .env.example docker-compose.yml CLAUDE.md .ai/worklog/2026-08-06-auth-implementation.md
git commit -m "docs(ai): auth deployment config + worklog"
```

---

## Self-Review（writing-plans 技能自检）

1. **Spec 覆盖**：需求文档 `.ai/requirements/2026-08-06-auth.md` 的每节都有对应任务——凭证模型→Task1/2；业务规则（fail-closed、常量比对、Cookie 标志、放行列表、防爆破）→Task3/4/5；API 路由→Task3/5；各端接入→Task6–10；测试策略→各任务 TDD；部署→Task11。
2. **占位符扫描**：无 TBD/TODO；所有代码块均为完整实现。
3. **类型一致性**：`authService` 方法名（`isAuthEnabled`/`authenticate`/`createSessionCookie`/`verifySessionCookie`/`login`）在 Task2 定义、Task3/5 使用，一致；domain 函数名在 Task1 定义、Task2/3/5 使用，一致；`TEST_AUTH_TOKEN` 在 Task2 helpers 定义、Task2/4/5 使用，一致。
